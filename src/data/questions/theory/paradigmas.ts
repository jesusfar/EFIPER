import { mc, ms, tf, withTopic } from '../builder';

// Banco TEÓRICO (a mano) — Paradigmas y Lenguajes (Java).
// Razonamiento: pilares de la POO, clases/objetos/constructores, static/final,
// sobrecarga vs sobrescritura, clase abstracta vs interfaz.
export const paradigmasTheory = withTopic('paradigmas_lenguajes', [
  mc('par-t-001', 'POO', 2,
    'Los cuatro pilares de la Programación Orientada a Objetos son…',
    [
      'Abstracción, encapsulamiento, herencia y polimorfismo',
      'Clases, objetos, métodos y atributos',
      'Variables, bucles, funciones y condicionales',
      'Compilación, ejecución, depuración y prueba',
    ], 0,
    'Los pilares de la POO son abstracción (modelar lo esencial), encapsulamiento (ocultar el estado), herencia (reutilizar) y polimorfismo (muchas formas).'),

  mc('par-t-002', 'POO', 2,
    'El encapsulamiento consiste en…',
    [
      'Ocultar el estado interno de un objeto y exponerlo solo mediante métodos',
      'Heredar atributos de una clase padre',
      'Que un método tome varias formas según el objeto',
      'Crear objetos a partir de una clase',
    ], 0,
    'El encapsulamiento protege los datos internos (atributos privados) y los expone de forma controlada vía getters/setters, preservando la integridad del objeto.'),

  mc('par-t-003', 'POO', 2,
    'La abstracción consiste en…',
    [
      'Modelar lo esencial de un objeto, ignorando los detalles irrelevantes al contexto',
      'Ocultar exclusivamente los atributos privados',
      'Crear muchas instancias de una clase',
      'Convertir un tipo de dato en otro',
    ], 0,
    'La abstracción se enfoca en las características esenciales relevantes al problema (el "qué hace"), dejando de lado la complejidad de la implementación.'),

  mc('par-t-004', 'POO', 2,
    'La herencia permite…',
    [
      'Que una subclase reutilice y extienda atributos y métodos de su superclase (relación "es-un")',
      'Que dos objetos no se relacionen entre sí',
      'Ocultar el estado interno de un objeto',
      'Crear constructores automáticamente',
    ], 0,
    'La herencia establece una relación "es-un" (un Gato ES UN Animal): la subclase hereda y puede extender o especializar el comportamiento de la superclase.'),

  mc('par-t-005', 'POO', 2,
    'El polimorfismo permite…',
    [
      'Que objetos de distintas clases respondan al mismo mensaje (método) de forma específica',
      'Que una clase tenga varios constructores',
      'Que un atributo sea privado',
      'Que una clase herede de varias clases a la vez',
    ], 0,
    'El polimorfismo ("muchas formas") permite que un mismo método se comporte distinto según la clase real del objeto que lo recibe (vía sobrescritura).'),

  ms('par-t-006', 'POO', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre los pilares de la POO:',
    [
      'El encapsulamiento oculta el estado interno y lo expone vía métodos',
      'La abstracción modela lo esencial, ignorando detalles irrelevantes',
      'La herencia permite reutilizar y extender (relación "es-un")',
      'El polimorfismo permite responder al mismo mensaje de forma específica',
      'La herencia consiste en ocultar el estado interno del objeto',
      'El polimorfismo es el mecanismo para crear objetos a partir de una clase',
    ], [0, 1, 2, 3],
    'Encapsulamiento (oculta estado), abstracción (lo esencial), herencia (reutiliza) y polimorfismo (muchas formas) son correctas. Ocultar el estado es encapsulamiento y crear objetos es instanciar, no polimorfismo.'),

  mc('par-t-007', 'Clases y objetos', 2,
    '¿Cuál es la diferencia entre una clase y un objeto?',
    [
      'La clase es la plantilla/molde; el objeto es una instancia concreta de esa clase',
      'El objeto es la plantilla y la clase la instancia',
      'Son exactamente lo mismo',
      'La clase tiene estado propio y el objeto no',
    ], 0,
    'La clase define la estructura y el comportamiento (atributos y métodos); el objeto es una instancia concreta con su propio estado, creada a partir de la clase.'),

  mc('par-t-008', 'Constructores', 2,
    'Un constructor es…',
    [
      'Un método especial que inicializa el objeto al instanciarlo (con new); lleva el nombre de la clase y no declara tipo de retorno',
      'Un método que destruye el objeto al final',
      'Un atributo privado de la clase',
      'Una interfaz que la clase implementa',
    ], 0,
    'El constructor inicializa el objeto al crearlo con new. Tiene el mismo nombre que la clase y no devuelve nada (ni siquiera void).'),

  tf('par-t-009', 'Constructores', 2,
    'En Java, un constructor lleva el mismo nombre que la clase y no declara ningún tipo de retorno (ni siquiera void).',
    true,
    'Verdadero. El constructor no especifica tipo de retorno: si se le pusiera void, dejaría de ser un constructor y pasaría a ser un método común con ese nombre.'),

  mc('par-t-010', 'Constructores', 3,
    'Si una clase no define ningún constructor, Java…',
    [
      'Provee automáticamente un constructor por defecto (sin parámetros)',
      'No permite instanciar la clase',
      'Genera un error de compilación',
      'Crea dos constructores distintos',
    ], 0,
    'El compilador agrega un constructor por defecto sin parámetros. Pero si definís al menos uno (con parámetros), ese por defecto ya no se genera automáticamente.'),

  mc('par-t-011', 'static y final', 3,
    'En Java, un atributo static…',
    [
      'Pertenece a la clase (es compartido por todas las instancias), no a cada objeto',
      'Es siempre una constante inmutable',
      'Es siempre privado',
      'No puede modificarse nunca',
    ], 0,
    'Un atributo static tiene una única copia compartida por todas las instancias; se accede mediante la clase (Clase.atributo), sin necesidad de un objeto.'),

  mc('par-t-012', 'static y final', 2,
    'En Java, la palabra clave final aplicada a una variable…',
    [
      'Hace que su valor no pueda modificarse una vez asignado (constante)',
      'La convierte en estática',
      'La hace privada',
      'Le permite tomar el valor null obligatoriamente',
    ], 0,
    'Una variable final se asigna una sola vez y luego es constante. (final en un método impide sobrescribirlo; en una clase, impide heredarla).'),

  ms('par-t-013', 'static y final', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre static y final:',
    [
      'static: el miembro pertenece a la clase y se comparte entre instancias',
      'final en una variable: su valor no puede cambiar (constante)',
      'final en un método: no se puede sobrescribir (override)',
      'final en una clase: no se puede heredar de ella',
      'static crea una copia del atributo por cada instancia',
      'final convierte el atributo en privado',
    ], [0, 1, 2, 3],
    'static (compartido por la clase), final en variable (constante), final en método (no override) y final en clase (no herencia) son correctas. static comparte una sola copia y final no afecta la visibilidad.'),

  mc('par-t-014', 'Sobrecarga y sobrescritura', 3,
    'La sobrecarga de métodos (overloading) consiste en…',
    [
      'Tener varios métodos con el mismo nombre pero distinta lista de parámetros',
      'Redefinir un método heredado en la subclase con la misma firma',
      'Declarar un método como abstracto',
      'Llamar a un método de forma recursiva',
    ], 0,
    'El overloading define varios métodos de igual nombre y firma distinta (cantidad/tipo de parámetros). Se resuelve en tiempo de compilación.'),

  mc('par-t-015', 'Sobrecarga y sobrescritura', 3,
    'La sobrescritura de métodos (overriding) consiste en…',
    [
      'Redefinir en la subclase un método de la superclase con la misma firma',
      'Tener varios métodos con distinta cantidad de parámetros',
      'Crear un constructor en la subclase',
      'Declarar el método como static',
    ], 0,
    'El overriding redefine en la subclase un método heredado, conservando la firma, para cambiar su comportamiento. Es la base del polimorfismo (enlace dinámico).'),

  tf('par-t-016', 'Sobrecarga y sobrescritura', 3,
    'La sobrecarga (overloading) redefine un método heredado con la misma firma, mientras que la sobrescritura (overriding) usa una lista de parámetros distinta.',
    false,
    'Falso. Está invertido: el OVERLOADING usa el mismo nombre con DISTINTA firma (en la misma clase); el OVERRIDING redefine en la subclase un método heredado con la MISMA firma.'),

  ms('par-t-017', 'Sobrecarga y sobrescritura', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre overloading y overriding:',
    [
      'La sobrecarga (overloading): mismo nombre, distinta lista de parámetros',
      'La sobrescritura (overriding): misma firma, redefinida en la subclase',
      'La sobrescritura es la base del polimorfismo',
      'La sobrecarga se resuelve en tiempo de compilación',
      'La sobrescritura (overriding) usa una lista de parámetros distinta',
      'La sobrecarga (overloading) redefine un método heredado de la superclase',
    ], [0, 1, 2, 3],
    'Overloading (distinta firma, compilación) y overriding (misma firma, subclase, polimorfismo) son correctas. Las dos falsas intercambian las definiciones de ambos conceptos.'),

  mc('par-t-018', 'Referencias', 2,
    'En Java, la palabra clave this…',
    [
      'Referencia a la instancia actual del objeto',
      'Referencia a la superclase',
      'Crea un objeto nuevo',
      'Es un tipo de dato primitivo',
    ], 0,
    'this apunta al objeto actual; se usa para distinguir atributos de parámetros con el mismo nombre o para invocar otro constructor de la misma clase.'),

  mc('par-t-019', 'Referencias', 3,
    'En Java, la palabra clave super se usa para…',
    [
      'Acceder a miembros (métodos o constructor) de la superclase',
      'Crear un objeto completamente nuevo',
      'Declarar un método como estático',
      'Ocultar los atributos de la clase',
    ], 0,
    'super permite invocar el constructor de la superclase (super(...)) o un método sobrescrito de la superclase desde la subclase.'),

  tf('par-t-020', 'Herencia', 2,
    'En Java, una clase puede heredar (extends) de varias clases a la vez.',
    false,
    'Falso. Java NO admite herencia múltiple de clases (para evitar el problema del diamante). Sí permite implementar múltiples interfaces.'),

  mc('par-t-021', 'Encapsulamiento', 2,
    'Los métodos getter y setter sirven para…',
    [
      'Acceder y modificar atributos privados de forma controlada (apoyan el encapsulamiento)',
      'Crear objetos a partir de la clase',
      'Heredar de una superclase',
      'Sobrecargar los constructores',
    ], 0,
    'Los getters/setters dan acceso regulado a los atributos privados: el setter puede validar antes de asignar, protegiendo la integridad del objeto.'),

  tf('par-t-022', 'Abstracción', 3,
    'En Java, una clase abstracta se puede instanciar directamente con new.',
    false,
    'Falso. Una clase abstracta NO se puede instanciar con new; sirve como base para subclases concretas. Puede contener métodos abstractos (sin cuerpo) y métodos concretos.'),

  mc('par-t-023', 'Abstracción', 3,
    'Una interfaz (en Java clásico) define…',
    [
      'Un contrato: los métodos que las clases que la implementan deben proveer',
      'Atributos con estado mutable propio',
      'Un constructor para crear instancias',
      'Una instancia concreta de un objeto',
    ], 0,
    'La interfaz declara un conjunto de métodos (el contrato) sin implementarlos (en su forma clásica); las clases que la implementan deben definir esos métodos.'),

  mc('par-t-024', 'Abstracción', 3,
    'La diferencia entre una clase abstracta y una interfaz (Java clásico) es que…',
    [
      'La clase abstracta puede tener estado y métodos implementados; la interfaz (clásica) solo define el contrato',
      'La interfaz puede instanciarse con new',
      'La clase abstracta no puede tener métodos',
      'Una clase puede heredar de varias clases abstractas a la vez',
    ], 0,
    'La clase abstracta puede tener atributos y métodos concretos; la interfaz clásica solo declara el contrato. Una clase hereda de UNA sola clase pero implementa VARIAS interfaces.'),

  ms('par-t-025', 'Abstracción', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre clases abstractas e interfaces:',
    [
      'Una clase abstracta no puede instanciarse directamente',
      'Una interfaz define un contrato de métodos a implementar',
      'Una clase puede implementar varias interfaces',
      'Una clase solo puede heredar (extends) de una única clase',
      'Una clase abstracta se puede instanciar con new',
      'Una clase puede heredar (extends) de varias clases a la vez',
    ], [0, 1, 2, 3],
    'Clase abstracta no instanciable, interfaz = contrato, varias interfaces y una sola superclase son correctas. Las falsas: la abstracta no se instancia y Java no tiene herencia múltiple de clases.'),

  mc('par-t-026', 'Java', 3,
    'En Java, la diferencia entre "==" y ".equals()" al comparar objetos es que…',
    [
      '"==" compara referencias (si son el mismo objeto); ".equals()" compara contenido si está sobrescrito',
      'Son equivalentes en todos los casos',
      '".equals()" compara referencias y "==" el contenido',
      '"==" no funciona con objetos',
    ], 0,
    '"==" verifica si dos referencias apuntan al mismo objeto en memoria; ".equals()" (bien sobrescrito) compara el contenido/valor lógico de los objetos.'),

  tf('par-t-027', 'Java', 3,
    'En Java, los tipos primitivos (int, double, boolean) no son objetos; para tratarlos como objetos se usan sus clases envoltorio (Integer, Double, Boolean).',
    true,
    'Verdadero. Los primitivos son tipos básicos (no objetos); los wrappers (Integer, Double, Boolean…) los "envuelven" como objetos cuando se necesita, por ejemplo en colecciones genéricas.'),

  mc('par-t-028', 'Encapsulamiento', 3,
    'Una ventaja del encapsulamiento es…',
    [
      'Proteger la integridad del objeto: el estado interno solo cambia mediante métodos controlados (con validación)',
      'Permitir la herencia múltiple de clases',
      'Hacer que el código se ejecute más lento',
      'Eliminar la necesidad de usar clases',
    ], 0,
    'Al exponer el estado solo mediante métodos, se puede validar y controlar cada cambio, evitando que el objeto quede en un estado inválido.'),

  tf('par-t-029', 'Polimorfismo', 3,
    'El polimorfismo (vía overriding) permite que, usando una referencia de la superclase, se ejecute el método de la subclase real en tiempo de ejecución (enlace dinámico).',
    true,
    'Verdadero. El enlace dinámico resuelve en ejecución qué versión del método llamar según el tipo REAL del objeto, no según el tipo de la referencia.'),

  ms('par-t-030', 'POO', 3,
    'Repaso: seleccioná TODAS las afirmaciones correctas sobre POO:',
    [
      'Un objeto es una instancia concreta de una clase',
      'El constructor inicializa el objeto al crearlo',
      'El encapsulamiento protege el estado interno del objeto',
      'El polimorfismo se apoya en la sobrescritura de métodos',
      'Una clase es una instancia de un objeto',
      'Java admite herencia múltiple de clases con extends',
    ], [0, 1, 2, 3],
    'Objeto = instancia, constructor inicializa, encapsulamiento protege y polimorfismo usa overriding son correctas. La clase NO es instancia del objeto (es al revés) y Java no tiene herencia múltiple de clases.'),

  mc('par-t-031', 'Tipos y operadores', 2,
    '¿Cuál de los siguientes es un tipo primitivo en Java?',
    [
      'int',
      'String',
      'Integer',
      'ArrayList',
    ], 0,
    'int es un tipo primitivo. String, Integer (wrapper) y ArrayList son clases (objetos), no primitivos.'),

  mc('par-t-032', 'Tipos y operadores', 2,
    'En Java, ¿cuál es el resultado de la expresión "7 / 2" si ambos operandos son int?',
    [
      '3',
      '3.5',
      '4',
      '3.0',
    ], 0,
    'La división entre dos int es entera: descarta la parte decimal. 7 / 2 = 3. Para obtener 3.5 habría que usar double.'),

  mc('par-t-033', 'Tipos y operadores', 2,
    'En Java, ¿cuál es el resultado de "17 % 5"?',
    [
      '2',
      '3',
      '3.4',
      '12',
    ], 0,
    'El operador % devuelve el resto de la división entera: 17 = 3×5 + 2, por lo tanto 17 % 5 = 2.'),

  mc('par-t-034', 'Tipos y operadores', 3,
    'Para obtener el resultado decimal de 7 dividido 2 en Java, hay que…',
    [
      'Castear a double: (double) 7 / 2 da 3.5',
      'Usar 7 % 2',
      'No es posible obtener decimales en Java',
      'Usar siempre tipos int',
    ], 0,
    'Si al menos un operando es double, la división es real: (double) 7 / 2 = 3.5. Con ambos int, el resultado se trunca a 3.'),

  tf('par-t-035', 'Tipos y operadores', 3,
    'En Java, asignar un valor double a una variable int se hace automáticamente, sin necesidad de un cast.',
    false,
    'Falso. Pasar de double a int es una conversión ESTRECHA (puede perder datos), así que requiere un cast explícito: (int). De int a double sí es automático (ampliación).'),

  mc('par-t-036', 'Tipos y operadores', 3,
    'La conversión de int a double en Java…',
    [
      'Es automática (ampliación/widening), sin pérdida de información',
      'Requiere un cast y pierde datos',
      'No es posible',
      'Es idéntica a convertir de double a int',
    ], 0,
    'int → double es una ampliación: cabe sin perder datos y el compilador la hace sola. El sentido inverso (double → int) sí necesita cast y puede truncar.'),

  mc('par-t-037', 'Tipos y operadores', 3,
    'En Java, ¿qué valor produce la expresión "(int) \'A\'"?',
    [
      '65 (su código en la tabla ASCII/Unicode)',
      "El carácter 'A'",
      '1',
      '0',
    ], 0,
    "El carácter 'A' tiene código 65; al castearlo a int se obtiene ese valor numérico. Los char se pueden tratar como enteros en operaciones aritméticas.",
  ),

  ms('par-t-038', 'Tipos y operadores', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre tipos y operadores en Java:',
    [
      'La división entre dos int es entera (descarta el decimal)',
      'El operador % devuelve el resto de la división',
      'La conversión de int a double es automática',
      'La conversión de double a int requiere un cast explícito',
      '"7 / 2" da 3.5 cuando ambos operandos son int',
      'La conversión de double a int es automática',
    ], [0, 1, 2, 3],
    'División int entera, % resto, int→double automático y double→int con cast son correctas. 7/2 con int da 3 (no 3.5) y double→int NO es automático.'),

  mc('par-t-039', 'Operadores', 3,
    'En Java, el operador && (AND lógico)…',
    [
      'Es de cortocircuito: si el primer operando es false, no evalúa el segundo',
      'Siempre evalúa ambos operandos',
      'Es un operador a nivel de bits',
      'Devuelve un número entero',
    ], 0,
    'El && cortocircuita: si el primer operando ya determina el resultado (false), no evalúa el segundo. Esto evita errores (ej. comprobar null antes de acceder a un método).'),

  mc('par-t-040', 'Control de flujo', 3,
    'En Java, si en un switch se omite el break en un caso…',
    [
      'La ejecución "cae" (fall-through) y continúa con el siguiente caso',
      'Salta automáticamente al final del switch',
      'Se produce un error de compilación',
      'Se repite el mismo caso',
    ], 0,
    'Sin break, la ejecución continúa en los casos siguientes (fall-through) hasta encontrar un break o el final del switch. A veces se usa a propósito, pero suele ser un error.'),

  mc('par-t-041', 'Control de flujo', 2,
    '¿Cuántas veces se ejecuta el cuerpo de "for (int i = 0; i < 5; i++)"?',
    [
      '5',
      '4',
      '6',
      'Infinitas',
    ], 0,
    'i toma los valores 0, 1, 2, 3 y 4 (mientras i < 5): el cuerpo se ejecuta 5 veces.'),

  tf('par-t-042', 'Control de flujo', 3,
    'Un bucle do-while ejecuta su cuerpo al menos una vez, ya que evalúa la condición DESPUÉS de ejecutarlo (a diferencia del while).',
    true,
    'Verdadero. El do-while ejecuta el cuerpo primero y luego evalúa la condición; el while la evalúa antes y, si es falsa, puede no ejecutarse ninguna vez.'),

  mc('par-t-043', 'String', 2,
    'En Java, los objetos String son…',
    [
      'Inmutables: no se pueden modificar después de creados',
      'Mutables: se pueden cambiar en su lugar',
      'Tipos primitivos',
      'Siempre null por defecto',
    ], 0,
    'Una vez creado, un String no cambia: cualquier "modificación" (concatenar, reemplazar) genera un nuevo objeto String. Por eso son seguros para compartir.'),

  mc('par-t-044', 'String', 3,
    'Cada concatenación de String con el operador "+"…',
    [
      'Crea un nuevo objeto String (por la inmutabilidad); para muchas concatenaciones conviene StringBuilder',
      'Modifica el String original en su lugar',
      'No consume memoria adicional',
      'Es más rápida que StringBuilder dentro de un bucle',
    ], 0,
    'Como String es inmutable, cada "+" crea un objeto nuevo; en un bucle eso genera muchos objetos temporales. StringBuilder modifica un buffer mutable, mucho más eficiente.'),

  mc('par-t-045', 'String', 3,
    'En Java, para comparar el CONTENIDO de dos String conviene usar…',
    [
      '.equals() (== compara referencias, no el contenido)',
      '== en todos los casos',
      'Los operadores > y <',
      'El operador +',
    ], 0,
    '.equals() compara los caracteres (el contenido); == compara si son el mismo objeto en memoria, lo que puede dar false aunque el texto sea igual.'),

  tf('par-t-046', 'String', 3,
    'En Java, comparar dos String con == siempre devuelve true si tienen el mismo contenido.',
    false,
    'Falso. == compara REFERENCIAS, no contenido: dos String con igual texto pero distintos objetos dan false. Para comparar el contenido se usa .equals().'),

  ms('par-t-047', 'String', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre String en Java:',
    [
      'String es inmutable (no cambia tras crearse)',
      '.equals() compara el contenido de los String',
      '== compara referencias (si son el mismo objeto)',
      'StringBuilder conviene para concatenar muchas veces (ej. en un bucle)',
      'String es mutable y se puede modificar en su lugar',
      '== compara el contenido de dos String',
    ], [0, 1, 2, 3],
    'String inmutable, .equals() (contenido), == (referencias) y StringBuilder (muchas concatenaciones) son correctas. String NO es mutable y == NO compara contenido.'),

  mc('par-t-048', 'Arrays', 2,
    'En Java, un arreglo (array)…',
    [
      'Tiene un tamaño fijo definido al crearse y se accede por índice desde 0',
      'Crece dinámicamente sin ningún límite',
      'No tiene índices',
      'Es exactamente lo mismo que un ArrayList',
    ], 0,
    'El array reserva un tamaño fijo al crearse (new int[n]) y se indexa desde 0 hasta n−1. Para colecciones que crecen se usa ArrayList.'),

  mc('par-t-049', 'Arrays', 3,
    'Acceder a un índice fuera del rango de un array en Java…',
    [
      'Lanza una excepción ArrayIndexOutOfBoundsException en tiempo de ejecución',
      'Devuelve null',
      'Devuelve 0',
      'Produce un error de compilación',
    ], 0,
    'Java verifica los límites en ejecución: acceder fuera de rango (ej. índice n en un array de tamaño n) lanza ArrayIndexOutOfBoundsException.'),

  mc('par-t-050', 'Arrays', 3,
    '¿Cuál es la diferencia entre un array y un ArrayList?',
    [
      'El array es de tamaño fijo; el ArrayList crece dinámicamente y ofrece métodos (add, remove, size)',
      'El ArrayList es de tamaño fijo y el array dinámico',
      'El array crece dinámicamente al agregar elementos',
      'Son estructuras idénticas',
    ], 0,
    'El array tiene tamaño fijo y es más eficiente; el ArrayList (del framework de colecciones) redimensiona solo y trae métodos cómodos, a costa de algo de overhead.'),

  tf('par-t-051', 'Arrays', 3,
    'En Java, los arreglos (arrays) tienen un tamaño fijo definido al crearse y no pueden cambiarlo después.',
    true,
    'Verdadero. El tamaño del array es fijo. Para una colección que crece o se achica dinámicamente se usa ArrayList u otra estructura del framework de colecciones.'),

  mc('par-t-052', 'Excepciones', 2,
    'En Java, el bloque try-catch sirve para…',
    [
      'Capturar y manejar excepciones que pueden ocurrir durante la ejecución',
      'Declarar variables locales',
      'Definir una nueva clase',
      'Crear un bucle de repetición',
    ], 0,
    'El código que puede fallar va en el try; si lanza una excepción, el catch correspondiente la captura y la maneja, evitando que el programa se detenga abruptamente.'),

  mc('par-t-053', 'Excepciones', 3,
    'El bloque finally en un try-catch…',
    [
      'Se ejecuta siempre, ocurra o no una excepción (ideal para liberar recursos)',
      'Solo se ejecuta si ocurre una excepción',
      'Solo se ejecuta si NO ocurre ninguna excepción',
      'Nunca se ejecuta',
    ], 0,
    'El finally corre pase lo que pase (haya excepción o no, e incluso con un return en el try), por eso es ideal para cerrar archivos, conexiones u otros recursos.'),

  mc('par-t-054', 'Excepciones', 3,
    '¿Cuál es la diferencia entre las excepciones checked y unchecked?',
    [
      'Las checked deben declararse o capturarse (el compilador lo exige); las unchecked (RuntimeException) no',
      'Las unchecked deben declararse obligatoriamente',
      'Son exactamente lo mismo',
      'Las checked solo ocurren en tiempo de compilación',
    ], 0,
    'Las checked (ej. IOException) obligan a manejarlas (try-catch o throws); las unchecked (RuntimeException, ej. NullPointer) no lo exigen, suelen ser errores de programación.'),

  mc('par-t-055', 'Excepciones', 3,
    'NullPointerException y ArrayIndexOutOfBoundsException son ejemplos de…',
    [
      'Excepciones unchecked (heredan de RuntimeException)',
      'Excepciones checked',
      'Errores de compilación',
      'Advertencias (warnings) del compilador',
    ], 0,
    'Ambas son RuntimeException (unchecked): el compilador no obliga a capturarlas; suelen indicar errores de lógica que conviene prevenir con validaciones.'),

  tf('par-t-056', 'Excepciones', 3,
    'El bloque finally solo se ejecuta cuando NO se lanzó ninguna excepción en el try.',
    false,
    'Falso. El finally se ejecuta SIEMPRE: haya o no excepción, e incluso si el try tiene un return. Por eso es el lugar ideal para liberar recursos.'),

  ms('par-t-057', 'Excepciones', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre el manejo de excepciones:',
    [
      'try-catch captura y maneja excepciones en tiempo de ejecución',
      'El bloque finally se ejecuta siempre, ocurra o no una excepción',
      'Las excepciones checked deben declararse o capturarse',
      'Las excepciones unchecked heredan de RuntimeException',
      'El bloque finally solo se ejecuta si ocurrió una excepción',
      'Las excepciones unchecked deben declararse obligatoriamente',
    ], [0, 1, 2, 3],
    'try-catch (captura), finally (siempre), checked (obligan a manejar) y unchecked (RuntimeException) son correctas. El finally corre siempre y las unchecked NO obligan a declararse.'),

  mc('par-t-058', 'Tipos y operadores', 3,
    'El autoboxing en Java es…',
    [
      'La conversión automática entre un primitivo (int) y su clase envoltorio (Integer)',
      'Convertir un objeto en primitivo manualmente con un cast',
      'La creación de un arreglo de objetos',
      'El lanzamiento de una excepción',
    ], 0,
    'El autoboxing convierte automáticamente un primitivo a su wrapper (int → Integer) y el unboxing al revés, lo que permite usar primitivos en colecciones genéricas (que guardan objetos).'),

  tf('par-t-059', 'Tipos y operadores', 3,
    'El autoboxing permite que un int se convierta automáticamente a Integer (y el unboxing al revés), lo que resulta útil al usar colecciones genéricas que solo aceptan objetos.',
    true,
    'Verdadero. Gracias al autoboxing/unboxing se pueden guardar primitivos en estructuras como ArrayList<Integer>, ya que las colecciones genéricas almacenan objetos, no primitivos.'),

  ms('par-t-060', 'Repaso', 3,
    'Repaso: seleccioná TODAS las afirmaciones correctas:',
    [
      'La división entre dos int trunca el decimal',
      'String es inmutable en Java',
      'El bloque finally se ejecuta siempre',
      'Los arrays tienen un tamaño fijo definido al crearse',
      '== compara el contenido de dos String',
      'Los arrays crecen dinámicamente al agregar elementos',
    ], [0, 1, 2, 3],
    'División int trunca, String inmutable, finally siempre y arrays de tamaño fijo son correctas. == compara referencias (no contenido) y los arrays NO crecen (eso es ArrayList).'),

  mc('par-t-061', 'Paradigmas', 2,
    'El paradigma imperativo se caracteriza por…',
    [
      'Describir paso a paso CÓMO realizar la tarea, mediante instrucciones que cambian el estado',
      'Describir QUÉ se quiere obtener, no el cómo',
      'Usar únicamente funciones puras',
      'Basarse en hechos y reglas lógicas',
    ], 0,
    'El paradigma imperativo detalla la secuencia de pasos que modifican el estado (variables). El declarativo, en cambio, describe el qué.'),

  mc('par-t-062', 'Paradigmas', 2,
    'El paradigma declarativo se caracteriza por…',
    [
      'Describir QUÉ se quiere obtener, sin especificar el cómo (ej. SQL)',
      'Describir el cómo paso a paso',
      'Modificar variables globales en bucles',
      'Usar únicamente estructuras de repetición',
    ], 0,
    'El declarativo expresa el resultado deseado y deja el "cómo" al motor/runtime (SQL, HTML, lenguajes funcionales y lógicos son ejemplos).'),

  mc('par-t-063', 'Paradigmas', 2,
    'El paradigma funcional se basa en…',
    [
      'Funciones puras, inmutabilidad y la ausencia de efectos secundarios',
      'Cambiar el estado mediante bucles',
      'Declarar hechos y reglas lógicas',
      'Objetos que colaboran enviándose mensajes',
    ], 0,
    'La programación funcional trata el cómputo como evaluación de funciones puras, evitando el estado mutable y los efectos secundarios, lo que facilita el razonamiento y la concurrencia.'),

  mc('par-t-064', 'Paradigmas', 3,
    'El paradigma lógico (por ejemplo, Prolog) se basa en…',
    [
      'Declarar hechos y reglas; el motor deduce las respuestas a las consultas',
      'Ejecutar instrucciones secuenciales que cambian el estado',
      'Usar exclusivamente funciones puras',
      'Modelar el problema con objetos y mensajes',
    ], 0,
    'En el paradigma lógico se describe el conocimiento (hechos y reglas) y el motor de inferencia deduce las soluciones, sin un algoritmo explícito paso a paso.'),

  ms('par-t-065', 'Paradigmas', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre los paradigmas de programación:',
    [
      'El paradigma imperativo describe el CÓMO, cambiando el estado',
      'El paradigma declarativo describe el QUÉ, no el cómo',
      'El paradigma funcional se basa en funciones puras e inmutabilidad',
      'La POO modela el sistema como objetos que colaboran',
      'El paradigma imperativo describe únicamente el QUÉ, no el cómo',
      'El paradigma funcional se basa en el estado mutable y los efectos secundarios',
    ], [0, 1, 2, 3],
    'Imperativo (cómo), declarativo (qué), funcional (puras/inmutable) y POO (objetos) son correctas. Las falsas confunden el imperativo con el declarativo y describen lo opuesto al funcional.'),

  tf('par-t-066', 'Paradigmas', 3,
    'Una función pura devuelve siempre la misma salida para la misma entrada y no produce efectos secundarios.',
    true,
    'Verdadero. La pureza (mismo resultado, sin tocar estado externo ni hacer E/S) es la base del paradigma funcional: facilita probar, razonar y paralelizar el código.'),

  tf('par-t-067', 'Paradigmas', 3,
    'Java es un lenguaje exclusivamente orientado a objetos y no admite ningún rasgo de otros paradigmas.',
    false,
    'Falso. Java es principalmente OOP, pero desde Java 8 incorpora rasgos FUNCIONALES (lambdas, streams, interfaces funcionales): es multiparadigma.'),

  mc('par-t-068', 'Colecciones', 2,
    'En Java, una List (por ejemplo ArrayList)…',
    [
      'Es una colección ORDENADA que admite elementos duplicados, accesible por índice',
      'No admite elementos duplicados',
      'No tiene ningún orden',
      'Asocia claves con valores',
    ], 0,
    'La List mantiene el orden de inserción, permite duplicados y acceso por índice. La que evita duplicados es Set; la de pares clave→valor es Map.'),

  mc('par-t-069', 'Colecciones', 2,
    'En Java, un Set (por ejemplo HashSet)…',
    [
      'Es una colección que NO admite elementos duplicados',
      'Admite elementos duplicados',
      'Está siempre ordenado por índice',
      'Asocia claves con valores',
    ], 0,
    'El Set modela un conjunto: cada elemento es único. HashSet no garantiza orden; TreeSet los mantiene ordenados.'),

  mc('par-t-070', 'Colecciones', 2,
    'En Java, un Map (por ejemplo HashMap)…',
    [
      'Asocia claves únicas con valores (clave → valor)',
      'Es una lista de elementos accesible por índice',
      'No permite buscar por clave',
      'Admite claves duplicadas',
    ], 0,
    'El Map guarda pares clave→valor con claves únicas y búsqueda rápida por clave. No es una secuencia indexada como la List.'),

  ms('par-t-071', 'Colecciones', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre las colecciones de Java:',
    [
      'List es ordenada (por índice) y admite duplicados',
      'Set no admite elementos duplicados',
      'Map asocia claves únicas con valores',
      'HashMap no garantiza un orden de iteración',
      'List no admite elementos duplicados',
      'Set asocia claves con valores (clave → valor)',
    ], [0, 1, 2, 3],
    'List (duplicados, índice), Set (sin duplicados), Map (clave→valor) y HashMap (sin orden) son correctas. La List SÍ admite duplicados y el Set NO asocia clave→valor (eso es Map).'),

  mc('par-t-072', 'Genéricos', 3,
    'Los genéricos (generics) en Java sirven para…',
    [
      'Escribir clases/métodos parametrizados por tipo, con seguridad de tipos en compilación (ej. List<String>)',
      'Crear objetos sin definir una clase',
      'Evitar por completo el uso de tipos',
      'Lanzar excepciones de forma automática',
    ], 0,
    'Los genéricos permiten parametrizar el tipo (List<String>, Map<Integer,Cliente>): el compilador verifica los tipos y se evitan casts manuales y errores en ejecución.'),

  mc('par-t-073', 'Genéricos', 3,
    'Una ventaja de los genéricos es…',
    [
      'Detectar errores de tipo en COMPILACIÓN y evitar casts manuales',
      'Hacer que el código se ejecute más lento',
      'Permitir cualquier tipo sin ningún control',
      'Eliminar la necesidad de usar clases',
    ], 0,
    'Con genéricos, un error de tipo se detecta al compilar (no en ejecución) y no hace falta castear al extraer elementos: el código es más seguro y legible.'),

  mc('par-t-074', 'Funcional', 3,
    'Una expresión lambda en Java…',
    [
      'Es una función anónima concisa que implementa una interfaz funcional (un solo método abstracto)',
      'Es una clase abstracta',
      'Es un tipo de dato primitivo',
      'Es una estructura de repetición',
    ], 0,
    'La lambda (parámetros -> cuerpo) provee la implementación del único método abstracto de una interfaz funcional, de forma compacta (ej. en streams, comparadores, callbacks).'),

  mc('par-t-075', 'Funcional', 3,
    'Una interfaz funcional es…',
    [
      'Una interfaz con un único método abstracto (puede implementarse con una lambda)',
      'Una interfaz con muchos métodos abstractos',
      'Una clase concreta instanciable',
      'Un tipo de dato primitivo',
    ], 0,
    'La interfaz funcional declara un solo método abstracto (ej. Runnable, Comparator, Function), lo que permite implementarla con una lambda o referencia a método.'),

  mc('par-t-076', 'Funcional', 3,
    'La API de Streams (Java 8) permite…',
    [
      'Procesar colecciones de forma declarativa con operaciones encadenadas (filter, map, reduce)',
      'Únicamente leer y escribir archivos',
      'Reemplazar a la JVM',
      'Crear hilos de forma automática',
    ], 0,
    'Los streams expresan el procesamiento de datos como un pipeline declarativo (filtrar, transformar, reducir), más legible que los bucles imperativos.'),

  tf('par-t-077', 'Funcional', 3,
    'Una expresión lambda puede usarse para implementar cualquier interfaz, aunque tenga varios métodos abstractos.',
    false,
    'Falso. La lambda solo sirve para interfaces FUNCIONALES (un único método abstracto). Si la interfaz tiene varios métodos abstractos, no puede expresarse con una lambda.'),

  ms('par-t-078', 'Funcional', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre la programación funcional en Java:',
    [
      'Una expresión lambda implementa una interfaz funcional',
      'Una interfaz funcional tiene un único método abstracto',
      'La API Streams procesa colecciones de forma declarativa',
      'La operación map transforma cada elemento del stream',
      'Una lambda puede implementar interfaces con muchos métodos abstractos',
      'Streams modifica directamente la colección original',
    ], [0, 1, 2, 3],
    'Lambda (interfaz funcional), un método abstracto, Streams declarativo y map (transforma) son correctas. La lambda solo aplica a interfaces de un método y Streams NO modifica la colección original.'),

  mc('par-t-079', 'Modificadores de acceso', 2,
    'El modificador de acceso public…',
    [
      'Hace al miembro accesible desde cualquier clase',
      'Lo hace accesible solo dentro de la propia clase',
      'Lo restringe al mismo paquete',
      'Lo restringe a las subclases',
    ], 0,
    'public da el acceso más abierto: el miembro es visible desde cualquier clase de cualquier paquete.'),

  mc('par-t-080', 'Modificadores de acceso', 2,
    'El modificador private…',
    [
      'Restringe el acceso al miembro solo dentro de la propia clase',
      'Lo hace accesible desde cualquier clase',
      'Lo hace accesible solo en las subclases',
      'Lo hace accesible en todo el paquete',
    ], 0,
    'private es el acceso más restrictivo: el miembro solo se puede usar dentro de la clase que lo define. Es clave para el encapsulamiento.'),

  mc('par-t-081', 'Modificadores de acceso', 3,
    'El modificador protected…',
    [
      'Permite el acceso desde la misma clase, el mismo paquete y las subclases (aun en otros paquetes)',
      'Solo permite acceso dentro de la propia clase',
      'Permite el acceso desde cualquier clase',
      'No permite el acceso desde ningún lado',
    ], 0,
    'protected amplía el acceso a las subclases (incluso en otros paquetes) además del propio paquete, útil para que las clases derivadas reutilicen miembros del padre.'),

  tf('par-t-082', 'Modificadores de acceso', 3,
    'En Java, un miembro sin modificador de acceso (default / package-private) es accesible desde cualquier clase de cualquier paquete.',
    false,
    'Falso. El acceso por defecto (sin modificador) es package-private: el miembro es accesible solo dentro del MISMO paquete, no desde cualquier paquete.'),

  ms('par-t-083', 'Modificadores de acceso', 3,
    'Seleccioná TODAS las correspondencias correctas de modificadores de acceso en Java:',
    [
      'public: accesible desde cualquier clase',
      'private: accesible solo dentro de la propia clase',
      'protected: accesible desde la clase, el paquete y las subclases',
      'Sin modificador (default): accesible solo dentro del mismo paquete',
      'private permite el acceso desde subclases en otro paquete',
      'public restringe el acceso al mismo paquete',
    ], [0, 1, 2, 3],
    'public (todos), private (la clase), protected (clase/paquete/subclases) y default (paquete) son correctas. private NO se ve desde subclases en otro paquete y public NO restringe al paquete.'),

  mc('par-t-084', 'Encapsulamiento', 2,
    'El encapsulamiento se logra en Java típicamente…',
    [
      'Declarando los atributos como private y exponiéndolos con getters/setters public',
      'Declarando todos los atributos como public',
      'Usando solo variables globales',
      'Evitando el uso de clases',
    ], 0,
    'El patrón habitual: atributos private (ocultos) + métodos public controlados (getters/setters) que pueden validar, protegiendo la integridad del objeto.'),

  mc('par-t-085', 'Paradigmas', 3,
    'Un ejemplo de programación declarativa es…',
    [
      'Una consulta SQL (se describe qué datos se quieren, no cómo obtenerlos)',
      'Un bucle for que recorre un arreglo elemento por elemento',
      'Modificar una variable acumuladora dentro de un ciclo',
      'Una secuencia de instrucciones que cambian el estado',
    ], 0,
    'SQL es declarativo: indicás qué datos querés (SELECT … WHERE …) y el motor decide cómo obtenerlos. Los bucles que cambian el estado son imperativos.'),

  tf('par-t-086', 'Paradigmas', 2,
    'Muchos lenguajes modernos (como Java, Python o JavaScript) son multiparadigma: combinan la orientación a objetos con rasgos funcionales.',
    true,
    'Verdadero. Estos lenguajes permiten programar con objetos y, a la vez, usar funciones de orden superior, lambdas e inmutabilidad: combinan paradigmas según convenga.'),

  tf('par-t-087', 'Funcional', 3,
    'Una ventaja de la inmutabilidad es que los objetos inmutables se pueden compartir entre hilos sin riesgo de condiciones de carrera.',
    true,
    'Verdadero. Si el estado no cambia, no hay escrituras concurrentes que coordinar: los objetos inmutables son seguros para compartir entre hilos sin sincronización.'),

  mc('par-t-088', 'Funcional', 3,
    'Un "efecto secundario" (side effect) es…',
    [
      'Cuando una función modifica algo fuera de su ámbito (estado global, E/S) además de devolver un valor',
      'Cuando una función no devuelve ningún valor',
      'Cuando una función se llama a sí misma (recursión)',
      'Cuando se lanza una excepción',
    ], 0,
    'Un efecto secundario es cualquier interacción observable con el exterior (modificar una variable global, escribir en pantalla/archivo). La programación funcional busca minimizarlos.'),

  mc('par-t-089', 'Colecciones', 3,
    'Necesitás guardar pares clave→valor con búsqueda rápida por clave. ¿Qué colección conviene?',
    [
      'Un Map (por ejemplo HashMap)',
      'Una List',
      'Un Set',
      'Un arreglo (array)',
    ], 0,
    'El Map está diseñado para asociar claves con valores y buscar por clave en O(1) promedio (HashMap). List, Set y array no ofrecen ese acceso por clave.'),

  ms('par-t-090', 'Repaso', 3,
    'Repaso de paradigmas y colecciones: seleccioná TODAS las afirmaciones correctas:',
    [
      'El paradigma funcional evita los efectos secundarios',
      'SQL es un ejemplo de lenguaje declarativo',
      'List admite elementos duplicados',
      'Map asocia claves con valores',
      'El paradigma imperativo describe únicamente el QUÉ',
      'Un Set permite elementos duplicados',
    ], [0, 1, 2, 3],
    'Funcional (sin efectos secundarios), SQL (declarativo), List (duplicados) y Map (clave→valor) son correctas. El imperativo describe el CÓMO y el Set NO permite duplicados.'),

  mc('par-t-091', 'Java', 2,
    'En Java, un enum (enumeración)…',
    [
      'Define un conjunto fijo de constantes con nombre (ej. los días de la semana)',
      'Es un tipo de dato primitivo',
      'Crece dinámicamente en tiempo de ejecución',
      'Es lo mismo que una interfaz',
    ], 0,
    'El enum representa un conjunto cerrado de valores constantes con nombre (LUNES, MARTES…), más seguro y legible que usar enteros o cadenas sueltas.'),

  mc('par-t-092', 'Patrones', 3,
    'El patrón Singleton en Java se implementa típicamente…',
    [
      'Con un constructor privado y un método estático que devuelve la única instancia',
      'Con varios constructores públicos',
      'Usando herencia múltiple',
      'Definiendo una interfaz con muchos métodos',
    ], 0,
    'El Singleton impide crear instancias desde afuera (constructor privado) y ofrece un único punto de acceso (getInstance()) que retorna siempre la misma instancia.'),

  mc('par-t-093', 'Java', 3,
    'En Java, si se sobrescribe el método equals()…',
    [
      'También se debe sobrescribir hashCode() para mantener el contrato (objetos iguales → mismo hash)',
      'No hace falta tocar hashCode()',
      'Se rompe la cadena de herencia',
      'equals() en realidad no se puede sobrescribir',
    ], 0,
    'El contrato exige que dos objetos iguales según equals() tengan el mismo hashCode(); si no, fallan en HashMap/HashSet. Por eso se sobrescriben ambos juntos.'),

  tf('par-t-094', 'Java', 3,
    'En Java, basta con sobrescribir equals(); no es necesario sobrescribir también hashCode().',
    false,
    'Falso. Si dos objetos son iguales según equals(), deben devolver el mismo hashCode(); de lo contrario se comportan mal en estructuras basadas en hash (HashMap/HashSet). Hay que sobrescribir AMBOS.'),

  mc('par-t-095', 'Java', 3,
    'En Java 8 y posteriores, una interfaz puede tener…',
    [
      'Métodos default (con implementación), además de los abstractos',
      'Únicamente constructores',
      'Estado mutable propio (atributos de instancia)',
      'Herencia múltiple de clases',
    ], 0,
    'Desde Java 8 las interfaces admiten métodos default (con cuerpo) y static, lo que permite evolucionar interfaces sin romper las clases que ya las implementan.'),

  mc('par-t-096', 'JVM y memoria', 2,
    'El recolector de basura (garbage collector) de Java…',
    [
      'Libera automáticamente la memoria de los objetos que ya no tienen referencias',
      'Compila el código fuente a bytecode',
      'Detecta los errores de sintaxis',
      'Optimiza las consultas SQL',
    ], 0,
    'El GC identifica los objetos inaccesibles (sin referencias) y recupera su memoria, evitando que el programador la gestione manualmente (a diferencia de C/C++).'),

  tf('par-t-097', 'JVM y memoria', 2,
    'El garbage collector de Java libera automáticamente la memoria de los objetos sin referencias, sin que el programador deba hacerlo manualmente.',
    true,
    'Verdadero. En Java no hay free()/delete: el recolector de basura recupera la memoria de los objetos inaccesibles, reduciendo fugas y errores de gestión manual.'),

  mc('par-t-098', 'JVM y memoria', 2,
    'La Máquina Virtual de Java (JVM)…',
    [
      'Ejecuta el bytecode, lo que hace a Java portable ("write once, run anywhere")',
      'Compila el código directamente al lenguaje máquina del procesador',
      'Es un editor de código fuente',
      'Es un motor de base de datos',
    ], 0,
    'La JVM interpreta/compila (JIT) el bytecode en cada plataforma; por eso el mismo .class corre en Windows, Linux o Mac sin recompilar el fuente.'),

  mc('par-t-099', 'JVM y memoria', 3,
    'El compilador de Java (javac) traduce el código fuente a…',
    [
      'Bytecode (.class), que luego ejecuta la JVM',
      'Lenguaje máquina del procesador directamente',
      'Código HTML',
      'Sentencias SQL',
    ], 0,
    'javac genera bytecode independiente de la plataforma; la JVM de cada sistema se encarga de ejecutarlo. Esa separación es la base de la portabilidad de Java.'),

  ms('par-t-100', 'JVM y memoria', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre la JVM y la gestión de memoria:',
    [
      'javac compila el código fuente a bytecode (.class)',
      'La JVM ejecuta el bytecode',
      'El garbage collector libera memoria automáticamente',
      'El bytecode permite la portabilidad de Java entre plataformas',
      'javac genera directamente código máquina del procesador',
      'El programador libera la memoria manualmente con delete',
    ], [0, 1, 2, 3],
    'javac→bytecode, la JVM lo ejecuta, el GC libera memoria y el bytecode da portabilidad: correctas. javac NO genera código máquina y en Java NO se libera memoria con delete.'),

  mc('par-t-101', 'Concurrencia', 3,
    'En Java, un hilo (Thread)…',
    [
      'Permite ejecutar tareas de forma concurrente/paralela dentro de un mismo proceso',
      'Es un tipo de excepción',
      'Es una colección de elementos',
      'Es un operador aritmético',
    ], 0,
    'Un Thread es una línea de ejecución independiente dentro del proceso; varios hilos comparten la memoria del proceso, por eso requieren sincronización al acceder a datos compartidos.'),

  mc('par-t-102', 'Concurrencia', 3,
    'La palabra clave synchronized en Java…',
    [
      'Controla el acceso concurrente a un recurso compartido, evitando condiciones de carrera',
      'Crea un nuevo hilo de ejecución',
      'Libera la memoria de un objeto',
      'Define una constante',
    ], 0,
    'synchronized garantiza que solo un hilo a la vez entre en el bloque/método sincronizado (exclusión mutua), protegiendo la sección crítica de condiciones de carrera.'),

  tf('par-t-103', 'Concurrencia', 3,
    'Compartir un objeto mutable entre varios hilos sin sincronización puede provocar condiciones de carrera (race conditions).',
    true,
    'Verdadero. Si varios hilos leen/escriben el mismo estado mutable sin coordinarse, el resultado depende del entrelazado de ejecución y se vuelve impredecible.'),

  mc('par-t-104', 'Concurrencia', 3,
    '¿Por qué los objetos inmutables (como String) son seguros entre hilos?',
    [
      'Porque su estado no cambia, así que no hay condiciones de carrera al compartirlos',
      'Porque son más rápidos de crear',
      'Porque ocupan menos memoria',
      'Porque no se pueden compartir entre hilos',
    ], 0,
    'Al no poder modificarse tras crearse, los objetos inmutables se comparten sin riesgo: no hay escrituras concurrentes que sincronizar.'),

  mc('par-t-105', 'Java', 3,
    '¿Por qué no se puede usar this dentro de un método static?',
    [
      'Porque un método static pertenece a la clase (no a una instancia) y this referencia a una instancia',
      'Porque this es una palabra reservada para constructores únicamente',
      'Porque static no existe en Java',
      'Porque this solo puede usarse en interfaces',
    ], 0,
    'Un método static se invoca sobre la clase, sin un objeto asociado; como this apunta a la instancia actual y no hay ninguna, no puede usarse allí.'),

  tf('par-t-106', 'Java', 3,
    'Un método static puede acceder directamente a los atributos no estáticos (de instancia) de la clase, sin necesidad de un objeto.',
    false,
    'Falso. Un método static no tiene instancia asociada (no hay this), así que NO puede acceder directamente a atributos de instancia: necesita un objeto concreto para hacerlo.'),

  mc('par-t-107', 'Polimorfismo', 3,
    'Si Animal es superclase de Perro, ¿es válida la sentencia "Animal a = new Perro();"?',
    [
      'Sí: una referencia de la superclase puede apuntar a un objeto de la subclase (upcasting)',
      'No: produce un error de compilación',
      'Solo si Animal es una clase final',
      'Solo usando siempre un cast explícito',
    ], 0,
    'El upcasting (referencia del supertipo apuntando a un objeto del subtipo) es válido y automático; es la base del polimorfismo: con esa referencia se llama al método sobrescrito del Perro.'),

  mc('par-t-108', 'Java', 3,
    'El operador instanceof en Java…',
    [
      'Verifica si un objeto es instancia de una clase o tipo determinado',
      'Crea una nueva instancia de la clase',
      'Convierte tipos primitivos entre sí',
      'Lanza una excepción de tipo',
    ], 0,
    'instanceof devuelve true si el objeto es del tipo indicado (o un subtipo); se usa, por ejemplo, antes de hacer un downcast seguro.'),

  ms('par-t-109', 'POO avanzado', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre POO avanzada en Java:',
    [
      'this no puede usarse dentro de un método static',
      'Un método static no puede acceder a atributos de instancia sin un objeto',
      'Una referencia de la superclase puede apuntar a un objeto de la subclase (upcasting)',
      'instanceof verifica si un objeto es de un tipo determinado',
      'Un método static accede directamente a los atributos de instancia',
      'La palabra this referencia a la superclase',
    ], [0, 1, 2, 3],
    'this fuera de static, sin acceso a instancia desde static, upcasting e instanceof son correctas. static no accede a atributos de instancia y this referencia a la INSTANCIA actual, no a la superclase (eso es super).'),

  mc('par-t-110', 'Operadores', 3,
    'En Java, ¿qué imprime el siguiente código: int x = 5; System.out.println(x++ + " " + x);?',
    [
      '5 6',
      '6 6',
      '5 5',
      '6 5',
    ], 0,
    'x++ es post-incremento: usa el valor actual (5) y LUEGO incrementa x a 6. Por eso imprime "5" (el x++ evaluado) y luego "6" (x ya incrementado).'),

  mc('par-t-111', 'Operadores', 3,
    '¿Cuál es la diferencia entre x++ y ++x?',
    [
      'x++ devuelve el valor ANTES de incrementar; ++x incrementa primero y devuelve el nuevo valor',
      'Son exactamente equivalentes',
      'x++ no incrementa la variable',
      '++x decrementa la variable',
    ], 0,
    'El post-incremento (x++) entrega el valor previo y después suma 1; el pre-incremento (++x) suma 1 primero y entrega el valor ya incrementado.'),

  tf('par-t-112', 'Java', 3,
    'En Java, los objetos se pasan a los métodos por referencia, por lo que reasignar el parámetro dentro del método cambia el objeto original del que llamó.',
    false,
    'Falso. Java pasa SIEMPRE por VALOR; para objetos, pasa una copia de la referencia. Se puede modificar el objeto apuntado, pero REASIGNAR el parámetro no afecta a la variable original del llamador.'),

  mc('par-t-113', 'Abstracción', 3,
    'Un método abstracto en Java…',
    [
      'Se declara sin cuerpo y debe implementarse en las subclases concretas',
      'Tiene una implementación completa en la clase abstracta',
      'Es siempre estático',
      'Es un tipo especial de constructor',
    ], 0,
    'El método abstracto define la firma sin cuerpo; obliga a las subclases concretas a implementarlo. Solo puede existir en clases abstractas o interfaces.'),

  mc('par-t-114', 'Abstracción', 3,
    '¿Cuándo conviene una interfaz por sobre una clase abstracta?',
    [
      'Cuando se quiere definir un contrato que clases no relacionadas puedan implementar (y una clase puede implementar varias)',
      'Cuando se necesita compartir estado y código común entre subclases',
      'Cuando se quiere instanciar directamente el tipo',
      'Nunca conviene usar una interfaz',
    ], 0,
    'La interfaz define un contrato sin atarse a una jerarquía; sirve cuando clases distintas deben compartir un comportamiento y aprovecha que una clase implementa varias interfaces. La clase abstracta es mejor para compartir estado/código.'),

  tf('par-t-115', 'Encapsulamiento', 2,
    'Declarar los atributos de una clase como private es una práctica clave del encapsulamiento.',
    true,
    'Verdadero. Hacer privados los atributos impide el acceso directo desde afuera; el estado solo cambia mediante métodos controlados (setters con validación), protegiendo la integridad del objeto.'),

  mc('par-t-116', 'Excepciones', 3,
    'En Java, todas las excepciones y errores heredan de…',
    [
      'La clase Throwable',
      'La clase Object únicamente',
      'La interfaz Runnable',
      'La clase String',
    ], 0,
    'Throwable es la raíz de la jerarquía de lanzables; de ella derivan Error (problemas graves) y Exception (con sus subtipos checked y unchecked/RuntimeException).'),

  mc('par-t-117', 'Excepciones', 3,
    '¿Cuál es la diferencia entre throw y throws?',
    [
      'throw lanza una excepción concreta; throws declara en la firma del método que éste puede lanzarla',
      'Son exactamente lo mismo',
      'throws lanza la excepción y throw la declara',
      'Ninguno se relaciona con las excepciones',
    ], 0,
    'throw new X() lanza efectivamente la excepción en un punto del código; throws X en la firma avisa a quien llame que el método puede propagar esa excepción.'),

  ms('par-t-118', 'Repaso', 3,
    'Repaso final: seleccioná TODAS las afirmaciones correctas sobre Java:',
    [
      'El garbage collector libera la memoria automáticamente',
      'La JVM ejecuta bytecode, dando portabilidad a Java',
      'Al sobrescribir equals() también se debe sobrescribir hashCode()',
      'Java pasa los argumentos por valor (una copia de la referencia, para objetos)',
      'En Java el programador libera la memoria manualmente con delete',
      'Java pasa los objetos por referencia: reasignar el parámetro cambia el original',
    ], [0, 1, 2, 3],
    'GC automático, JVM ejecuta bytecode, equals()+hashCode() y paso por valor son correctas. En Java no se libera memoria con delete y NO se pasa por referencia (reasignar el parámetro no afecta al original).'),

  mc('par-t-119', 'Patrones', 3,
    'Querés que una clase tenga UNA sola instancia compartida en toda la aplicación (por ejemplo, la configuración). ¿Qué patrón usás?',
    [
      'Singleton',
      'Observer',
      'Factory Method',
      'Adapter',
    ], 0,
    'El Singleton garantiza una única instancia con acceso global controlado, ideal para configuraciones, loggers o pools que deben ser únicos en la aplicación.'),

  mc('par-t-120', 'Repaso', 3,
    '¿Cuál afirmación integradora sobre Java y la POO es correcta?',
    [
      'Java es multiparadigma (OOP + funcional desde Java 8), compila a bytecode que ejecuta la JVM y gestiona la memoria con un garbage collector',
      'Java compila directo a código máquina y obliga a liberar la memoria manualmente',
      'Java solo soporta el paradigma imperativo',
      'Java no tiene tipos de datos definidos',
    ], 0,
    'Java combina objetos y rasgos funcionales, logra portabilidad con bytecode + JVM y libera memoria automáticamente con el GC: esas tres ideas resumen el lenguaje.'),
]);
