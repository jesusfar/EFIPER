import { mc, ms, tf, withTopic } from '../builder';

// Banco TEÓRICO (a mano) — Bases de Datos.
// Razonamiento: claves, SQL (filtrado/agrupamiento/JOINs), integridad,
// normalización y transacciones ACID.
export const basesDatosTheory = withTopic('base_de_datos', [
  mc('bd-t-001', 'Claves', 2,
    '¿Cuál de estas afirmaciones sobre la clave primaria (PK) es correcta?',
    [
      'Es única, no admite valores nulos e identifica cada fila de la tabla',
      'Puede repetirse mientras no sea nula',
      'Siempre es un número entero autoincremental',
      'Referencia la clave primaria de otra tabla',
    ], 0,
    'La PK identifica unívocamente cada fila: debe ser única y no nula. La que referencia la PK de otra tabla es la clave foránea.'),

  mc('bd-t-002', 'Claves', 2,
    'Una clave foránea (FK)…',
    [
      'Es un atributo que referencia la PK de otra tabla y garantiza la integridad referencial',
      'Identifica unívocamente cada fila de su propia tabla',
      'Nunca puede tomar el valor nulo',
      'Siempre coincide con la PK de su propia tabla',
    ], 0,
    'La FK apunta a la PK de otra tabla, manteniendo la integridad referencial. Puede ser nula (si se permite) y no es la que identifica su propia tabla.'),

  mc('bd-t-003', 'Claves', 3,
    'Una clave candidata es…',
    [
      'Cualquier atributo o conjunto mínimo de atributos que podría servir como clave primaria',
      'Un atributo cuyos valores se repiten',
      'La clave foránea de la tabla',
      'Un índice secundario sin relación con la identidad',
    ], 0,
    'Las claves candidatas identifican unívocamente las filas; de entre ellas se elige una como PK y las demás quedan como claves alternativas.'),

  mc('bd-t-004', 'Claves', 3,
    'Una clave primaria compuesta…',
    [
      'Está formada por dos o más atributos que, juntos, identifican unívocamente cada fila',
      'Es una PK que admite valores nulos',
      'Es lo mismo que una clave foránea',
      'Es una clave cuyos valores pueden repetirse',
    ], 0,
    'La PK compuesta combina varios atributos para lograr unicidad; es típica de las tablas intermedias de relaciones N:M (ej. id_alumno + id_materia).'),

  ms('bd-t-005', 'Claves', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre claves:',
    [
      'La clave primaria es única y no admite nulos',
      'La clave foránea referencia la PK de otra tabla',
      'Una tabla puede tener varias claves candidatas',
      'Las claves candidatas no elegidas como PK quedan como claves alternativas',
      'La clave primaria puede contener valores nulos',
      'Una tabla puede tener varias claves primarias a la vez',
    ], [0, 1, 2, 3],
    'PK única y no nula; FK referencia otra PK; puede haber varias candidatas (una es PK, el resto alternativas). Una PK no admite nulos y hay una sola PK por tabla.'),

  mc('bd-t-006', 'SQL', 2,
    '¿Qué devuelve "SELECT nombre FROM Cliente WHERE ciudad = \'Córdoba\';"?',
    [
      'Los nombres de los clientes cuya ciudad es Córdoba',
      'Todos los clientes de la tabla',
      'La cantidad de clientes de Córdoba',
      'Las ciudades distintas de los clientes',
    ], 0,
    'El SELECT proyecta la columna nombre y el WHERE filtra las filas cuya ciudad sea Córdoba.'),

  mc('bd-t-007', 'SQL', 3,
    '¿Cuál es la diferencia entre WHERE y HAVING?',
    [
      'WHERE filtra filas antes de agrupar; HAVING filtra grupos después de GROUP BY (puede usar agregados)',
      'Son sinónimos intercambiables',
      'HAVING filtra antes de agrupar y WHERE después',
      'WHERE solo funciona con funciones de agregado',
    ], 0,
    'WHERE actúa fila por fila ANTES del agrupamiento; HAVING filtra los grupos ya formados y admite agregados como COUNT o SUM.'),

  mc('bd-t-008', 'SQL', 3,
    '¿Qué devuelve "SELECT ciudad, COUNT(*) FROM Cliente GROUP BY ciudad;"?',
    [
      'La cantidad de clientes por cada ciudad',
      'Todos los clientes ordenados por ciudad',
      'Un único número con el total de clientes',
      'Las ciudades sin contar nada',
    ], 0,
    'GROUP BY ciudad agrupa las filas por ciudad y COUNT(*) cuenta cuántos clientes hay en cada grupo.'),

  mc('bd-t-009', 'JOIN', 3,
    'Un INNER JOIN entre A y B devuelve…',
    [
      'Solo las filas que tienen coincidencia en ambas tablas',
      'Todas las filas de A, coincidan o no con B',
      'Todas las filas de B, coincidan o no con A',
      'El producto cartesiano completo de A y B',
    ], 0,
    'El INNER JOIN devuelve únicamente las combinaciones donde la condición de unión se cumple en ambas tablas; las filas sin pareja se descartan.'),

  mc('bd-t-010', 'JOIN', 3,
    'Un LEFT JOIN entre A y B devuelve…',
    [
      'Todas las filas de A y las coincidentes de B (con NULL donde no hay coincidencia)',
      'Solo las filas con coincidencia en ambas',
      'Todas las filas de B y las coincidentes de A',
      'El producto cartesiano de A y B',
    ], 0,
    'El LEFT JOIN conserva TODAS las filas de la tabla izquierda (A); donde no hay match en B, las columnas de B quedan en NULL.'),

  ms('bd-t-011', 'JOIN', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre las uniones (JOIN):',
    [
      'INNER JOIN devuelve solo las filas con coincidencia en ambas tablas',
      'LEFT JOIN conserva todas las filas de la tabla izquierda',
      'El producto cartesiano combina cada fila de A con cada fila de B',
      'FULL OUTER JOIN conserva las filas no coincidentes de ambas tablas',
      'INNER JOIN conserva también las filas sin coincidencia',
      'LEFT JOIN descarta las filas de la izquierda que no tienen coincidencia',
    ], [0, 1, 2, 3],
    'INNER = solo coincidencias; LEFT = todas las izquierdas; producto cartesiano = todas las combinaciones; FULL OUTER = ambas con sus no coincidentes. Las dos falsas contradicen INNER y LEFT.'),

  mc('bd-t-012', 'Transacciones', 3,
    'La propiedad de Atomicidad (ACID) garantiza que…',
    [
      'Una transacción se ejecute completamente o no se ejecute en absoluto (todo o nada)',
      'Los datos cumplan las reglas de integridad',
      'Las transacciones concurrentes no se interfieran',
      'Los cambios persistan tras un fallo',
    ], 0,
    'Atomicidad = todo o nada: si una parte falla, se deshace (rollback) toda la transacción. Es la A de ACID.'),

  tf('bd-t-013', 'Transacciones', 3,
    'La propiedad de Consistencia (ACID) garantiza que una transacción lleve la base de datos de un estado válido a otro estado válido, respetando todas las reglas e integridades.',
    true,
    'Verdadero. La Consistencia asegura que, al confirmar, no se violen las restricciones (claves, checks, integridad referencial): de estado válido a estado válido.'),

  mc('bd-t-014', 'Transacciones', 3,
    'La propiedad de Aislamiento (Isolation) garantiza que…',
    [
      'Las transacciones concurrentes no interfieran entre sí (el resultado es como si se ejecutaran en serie)',
      'La transacción se ejecute completa o nada',
      'Se cumplan las reglas de integridad',
      'Los cambios persistan tras un fallo del sistema',
    ], 0,
    'El Aislamiento controla la concurrencia: cada transacción se ejecuta como si estuviera sola, evitando que los cambios intermedios de una afecten a otra.'),

  mc('bd-t-015', 'Transacciones', 3,
    'La propiedad de Durabilidad (ACID) garantiza que…',
    [
      'Una vez confirmada (commit), los cambios persisten aunque ocurra un fallo del sistema',
      'La transacción se ejecute completa o nada',
      'Las transacciones no se interfieran',
      'La base pase de un estado válido a otro válido',
    ], 0,
    'La Durabilidad asegura que lo confirmado quede grabado de forma permanente (en disco/log), sobreviviendo a cortes de energía o caídas.'),

  ms('bd-t-016', 'Transacciones', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre ACID:',
    [
      'Atomicidad: la transacción se ejecuta completa o no se ejecuta',
      'Consistencia: lleva la base de un estado válido a otro válido',
      'Aislamiento: las transacciones concurrentes no interfieren entre sí',
      'Durabilidad: los cambios confirmados persisten ante fallos',
      'Atomicidad significa que los datos persisten tras un fallo',
      'Durabilidad significa "todo o nada"',
    ], [0, 1, 2, 3],
    'A=todo o nada; C=estado válido a válido; I=concurrencia sin interferencia; D=persistencia tras commit. Las dos falsas intercambian Atomicidad y Durabilidad.'),

  tf('bd-t-017', 'Transacciones', 2,
    'Si una transacción falla a la mitad, se realiza un ROLLBACK para deshacer todos sus cambios y preservar la atomicidad.',
    true,
    'Verdadero. El rollback revierte la transacción incompleta, dejando la base como si nunca hubiera empezado: así se cumple el "todo o nada".'),

  tf('bd-t-018', 'Integridad', 3,
    'La integridad referencial impide insertar en una tabla hija un valor de clave foránea que no exista como clave primaria en la tabla padre.',
    true,
    'Verdadero. No se puede referenciar algo inexistente: la FK debe apuntar a una PK existente (o ser NULL, si la columna lo permite).'),

  mc('bd-t-019', 'Normalización', 3,
    'Una tabla Pedido con columnas (id, cliente, producto1, producto2, producto3) viola…',
    [
      'La 1FN, por tener grupos repetitivos (los productos repartidos en columnas)',
      'Únicamente la 3FN',
      'Ninguna forma normal',
      'La integridad referencial',
    ], 0,
    'Repetir un mismo concepto en columnas (producto1, producto2…) es un grupo repetitivo: viola la 1FN. Se corrige con una tabla de detalle (un producto por fila).'),

  mc('bd-t-020', 'SQL', 2,
    '¿Qué devuelve "SELECT DISTINCT ciudad FROM Cliente;"?',
    [
      'Las ciudades sin repetir (valores únicos)',
      'Todas las filas, incluyendo ciudades repetidas',
      'La cantidad de ciudades',
      'Los clientes ordenados por ciudad',
    ], 0,
    'DISTINCT elimina duplicados del resultado: devuelve cada ciudad una sola vez.'),

  mc('bd-t-021', 'SQL', 2,
    'La cláusula ORDER BY se utiliza para…',
    [
      'Ordenar el resultado por una o más columnas (ascendente o descendente)',
      'Filtrar las filas según una condición',
      'Agrupar las filas por un campo',
      'Unir dos tablas',
    ], 0,
    'ORDER BY ordena el conjunto de resultados (ASC por defecto, o DESC). No filtra (eso es WHERE) ni agrupa (eso es GROUP BY).'),

  mc('bd-t-022', 'SQL', 2,
    '¿Cuál función de agregación devuelve el promedio de los valores de una columna?',
    [
      'AVG()',
      'SUM()',
      'COUNT()',
      'MAX()',
    ], 0,
    'AVG() calcula el promedio. SUM() suma, COUNT() cuenta filas y MAX() devuelve el valor máximo.'),

  ms('bd-t-023', 'SQL', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre las cláusulas SQL:',
    [
      'WHERE filtra filas (antes de agrupar)',
      'GROUP BY agrupa las filas por uno o más campos',
      'HAVING filtra grupos (después de GROUP BY)',
      'ORDER BY ordena el resultado final',
      'HAVING filtra filas individuales antes de agrupar',
      'WHERE se usa para ordenar el resultado',
    ], [0, 1, 2, 3],
    'WHERE filtra filas, GROUP BY agrupa, HAVING filtra grupos y ORDER BY ordena. HAVING no filtra filas pre-agrupamiento (eso es WHERE) y WHERE no ordena (eso es ORDER BY).'),

  mc('bd-t-024', 'SQL', 3,
    'En SQL, comparar un valor con NULL usando "columna = NULL"…',
    [
      'No funciona como se espera; hay que usar IS NULL / IS NOT NULL',
      'Devuelve siempre verdadero',
      'Es la forma correcta y recomendada',
      'Convierte el NULL en cero automáticamente',
    ], 0,
    'NULL representa "desconocido": cualquier comparación con = NULL da UNKNOWN, no TRUE. Para evaluar nulos se usa IS NULL o IS NOT NULL.'),

  tf('bd-t-025', 'Integridad', 2,
    'Una restricción UNIQUE impide que se repitan valores en una columna, aunque (según el motor) puede permitir un valor NULL.',
    true,
    'Verdadero. UNIQUE garantiza unicidad de valores; a diferencia de la PK, suele admitir un NULL (que no se considera duplicado de otro NULL en varios motores).'),

  mc('bd-t-026', 'Índices', 3,
    'Un índice en una base de datos sirve principalmente para…',
    [
      'Acelerar las búsquedas/consultas, a costa de espacio extra y escrituras más lentas',
      'Reducir el tamaño total de la tabla',
      'Cifrar los datos almacenados',
      'Garantizar la atomicidad de las transacciones',
    ], 0,
    'El índice acelera SELECT/WHERE/JOIN, pero ocupa espacio y agrega trabajo a INSERT/UPDATE/DELETE, que deben mantenerlo actualizado.'),

  tf('bd-t-027', 'Índices', 3,
    'Un índice acelera las lecturas, pero ralentiza los INSERT/UPDATE/DELETE porque debe mantenerse actualizado en cada cambio.',
    true,
    'Verdadero. Cada escritura debe actualizar también el índice; por eso indexar de más perjudica el rendimiento de las operaciones de modificación.'),

  mc('bd-t-028', 'Vistas', 2,
    'Una vista (VIEW) en SQL es…',
    [
      'Una tabla virtual definida por una consulta; no almacena datos propios (salvo que sea materializada)',
      'Una copia física de una tabla',
      'Un tipo especial de índice',
      'Un respaldo (backup) de la base de datos',
    ], 0,
    'La vista es una consulta almacenada que se comporta como una tabla: al consultarla, obtiene los datos en el momento (las materializadas sí guardan el resultado).'),

  mc('bd-t-029', 'SQL', 3,
    'Una subconsulta (subquery)…',
    [
      'Es una consulta anidada dentro de otra, cuyo resultado utiliza la consulta externa',
      'Es un tipo de índice',
      'Es una restricción de integridad',
      'Es una transacción',
    ], 0,
    'La subconsulta se ejecuta dentro de otra (en el WHERE, FROM o SELECT) y su resultado alimenta a la consulta externa, p. ej. para filtrar por un valor calculado.'),

  mc('bd-t-030', 'SQL', 2,
    '¿Qué devuelve "SELECT COUNT(*) FROM Venta WHERE total > 1000;"?',
    [
      'La cantidad de ventas cuyo total supera 1000',
      'La suma de todos los totales',
      'Todas las ventas mayores a 1000',
      'El promedio de los totales',
    ], 0,
    'El WHERE filtra las ventas con total > 1000 y COUNT(*) cuenta cuántas filas quedan: la cantidad de esas ventas.'),

  mc('bd-t-031', 'Lenguajes SQL', 3,
    '¿Cuál de las siguientes clasificaciones de SQL es correcta?',
    [
      'DDL define la estructura (CREATE/ALTER/DROP); DML manipula datos (INSERT/UPDATE/DELETE/SELECT); DCL controla permisos (GRANT/REVOKE)',
      'DDL manipula los datos y DML define la estructura',
      'DCL se usa para crear tablas',
      'Todas las sentencias SQL son DML',
    ], 0,
    'DDL (definición), DML (manipulación de datos) y DCL (control de acceso) son las grandes categorías; COMMIT/ROLLBACK forman el TCL (control de transacciones).'),

  mc('bd-t-032', 'Lenguajes SQL', 2,
    'CREATE TABLE, ALTER TABLE y DROP TABLE son sentencias de tipo…',
    [
      'DDL (Data Definition Language)',
      'DML (Data Manipulation Language)',
      'DCL (Data Control Language)',
      'TCL (Transaction Control Language)',
    ], 0,
    'Definen o modifican la ESTRUCTURA de la base (tablas, columnas, restricciones): son DDL.'),

  tf('bd-t-033', 'Lenguajes SQL', 2,
    'INSERT, UPDATE y DELETE son sentencias DML (Lenguaje de Manipulación de Datos).',
    true,
    'Verdadero. El DML opera sobre los DATOS de las tablas (agregar, modificar, borrar y consultar), a diferencia del DDL que define la estructura.'),

  ms('bd-t-034', 'Lenguajes SQL', 3,
    'Seleccioná TODAS las clasificaciones correctas de sentencias SQL:',
    [
      'CREATE TABLE es una sentencia DDL',
      'INSERT es una sentencia DML',
      'GRANT es una sentencia DCL (control de permisos)',
      'COMMIT y ROLLBACK controlan transacciones (TCL)',
      'DROP TABLE es una sentencia DML',
      'SELECT es una sentencia DDL',
    ], [0, 1, 2, 3],
    'CREATE=DDL, INSERT=DML, GRANT=DCL, COMMIT/ROLLBACK=TCL. DROP es DDL (estructura), no DML, y SELECT es DML/DQL (consulta), no DDL.'),

  mc('bd-t-035', 'Lenguajes SQL', 3,
    '¿Cuál es la diferencia entre TRUNCATE y DELETE?',
    [
      'TRUNCATE borra todas las filas rápidamente y sin WHERE (no registra fila por fila); DELETE borra filas (admite WHERE y es transaccional)',
      'TRUNCATE elimina la estructura de la tabla',
      'DELETE elimina la estructura de la tabla',
      'Son operaciones idénticas',
    ], 0,
    'TRUNCATE vacía la tabla de forma masiva (se comporta como DDL); DELETE borra filas seleccionables con WHERE y puede deshacerse en una transacción. Ninguno borra la estructura (eso es DROP).'),

  tf('bd-t-036', 'Lenguajes SQL', 2,
    'GRANT otorga permisos y REVOKE los quita a los usuarios sobre los objetos de la base de datos.',
    true,
    'Verdadero. GRANT y REVOKE son sentencias DCL: administran qué puede hacer cada usuario (SELECT, INSERT, etc.) sobre tablas, vistas y demás objetos.'),

  mc('bd-t-037', 'Concurrencia', 3,
    'Un bloqueo (lock) en una base de datos sirve para…',
    [
      'Controlar el acceso concurrente a los datos, evitando que dos transacciones los modifiquen a la vez de forma inconsistente',
      'Cifrar los datos almacenados',
      'Acelerar las consultas de lectura',
      'Crear índices automáticamente',
    ], 0,
    'Los locks coordinan la concurrencia: aseguran que las transacciones no pisen los cambios de otras, manteniendo el aislamiento y la consistencia.'),

  mc('bd-t-038', 'Concurrencia', 3,
    'Un interbloqueo (deadlock) en una base de datos ocurre cuando…',
    [
      'Dos transacciones se bloquean mutuamente, cada una esperando un recurso que la otra tiene bloqueado',
      'Una consulta tarda demasiado en ejecutarse',
      'Se llena el espacio en disco',
      'Falta un índice en una tabla',
    ], 0,
    'En el deadlock T1 espera un recurso bloqueado por T2 y viceversa: espera circular. El motor detecta el ciclo y aborta una de las transacciones.'),

  tf('bd-t-039', 'Concurrencia', 3,
    'Cuando el motor detecta un deadlock, suele abortar una de las transacciones (la "víctima") y deshacer sus cambios para resolver el bloqueo.',
    true,
    'Verdadero. El gestor elige una víctima (normalmente la de menor costo de rollback), la revierte y la otra continúa; la víctima puede reintentarse.'),

  mc('bd-t-040', 'Concurrencia', 3,
    'Una "lectura sucia" (dirty read) ocurre cuando…',
    [
      'Una transacción lee datos modificados por otra que todavía no confirmó (y que podría hacer rollback)',
      'Una transacción lee solo datos ya confirmados',
      'Se elimina una tabla durante la lectura',
      'Dos transacciones leen exactamente el mismo dato a la vez',
    ], 0,
    'En la lectura sucia se lee un cambio aún no confirmado; si la otra transacción hace rollback, se habrá leído un dato que "nunca existió".'),

  mc('bd-t-041', 'Concurrencia', 3,
    'Una "lectura no repetible" (non-repeatable read) ocurre cuando…',
    [
      'Una transacción lee la misma fila dos veces y obtiene valores distintos, porque otra la modificó y confirmó entre ambas lecturas',
      'Aparecen filas nuevas que antes no estaban',
      'Se lee un dato no confirmado',
      'No se puede leer la fila',
    ], 0,
    'La fila existe en ambas lecturas, pero su valor cambió porque otra transacción la actualizó y confirmó en el medio: la relectura no coincide.'),

  mc('bd-t-042', 'Concurrencia', 3,
    'Una "lectura fantasma" (phantom read) ocurre cuando…',
    [
      'Una consulta con una condición devuelve un conjunto distinto de filas al repetirse, porque otra transacción insertó o eliminó filas que cumplen esa condición',
      'Se lee un valor cambiado de una fila existente',
      'Se lee un dato no confirmado',
      'Se bloquea toda la tabla',
    ], 0,
    'A diferencia de la no repetible (cambia un valor), en la fantasma cambia el CONJUNTO de filas: aparecen o desaparecen filas que cumplen la condición.'),

  ms('bd-t-043', 'Concurrencia', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre los fenómenos de concurrencia:',
    [
      'La lectura sucia (dirty read) es leer datos no confirmados de otra transacción',
      'La lectura no repetible es releer una fila y obtener valores distintos',
      'La lectura fantasma es que aparezcan/desaparezcan filas entre dos consultas',
      'Subir el nivel de aislamiento reduce estos fenómenos pero baja la concurrencia',
      'La lectura sucia consiste en leer datos ya confirmados',
      'El nivel Serializable permite las lecturas sucias',
    ], [0, 1, 2, 3],
    'Dirty=no confirmado; non-repeatable=valor distinto; phantom=filas que aparecen/desaparecen; más aislamiento = menos fenómenos y menos concurrencia. Las dos falsas se contradicen (Serializable evita todo).'),

  mc('bd-t-044', 'Concurrencia', 3,
    'Ordená los niveles de aislamiento de MENOR a MAYOR aislamiento:',
    [
      'Read Uncommitted < Read Committed < Repeatable Read < Serializable',
      'Serializable < Repeatable Read < Read Committed < Read Uncommitted',
      'Read Committed < Read Uncommitted < Serializable < Repeatable Read',
      'Repeatable Read < Serializable < Read Committed < Read Uncommitted',
    ], 0,
    'De más permisivo a más estricto: Read Uncommitted (permite dirty), Read Committed, Repeatable Read y Serializable (evita todos los fenómenos).'),

  mc('bd-t-045', 'Concurrencia', 3,
    'El nivel de aislamiento Serializable…',
    [
      'Es el más estricto: evita dirty, non-repeatable y phantom reads, a costa de menor concurrencia',
      'Es el más permisivo de todos',
      'Permite las lecturas sucias',
      'No está definido por el estándar SQL',
    ], 0,
    'Serializable hace que las transacciones se comporten como si se ejecutaran en serie: máxima consistencia, pero más bloqueos y menor rendimiento concurrente.'),

  tf('bd-t-046', 'Concurrencia', 3,
    'A mayor nivel de aislamiento se obtiene mayor consistencia, pero menor concurrencia y rendimiento.',
    true,
    'Verdadero. Es un compromiso: más aislamiento evita anomalías pero exige más bloqueos, reduciendo cuántas transacciones avanzan en paralelo.'),

  mc('bd-t-047', 'SQL', 3,
    'El operador UNION en SQL…',
    [
      'Combina los resultados de dos consultas eliminando duplicados (UNION ALL los conserva)',
      'Une dos tablas relacionándolas por una columna',
      'Filtra filas según una condición',
      'Ordena el resultado de una consulta',
    ], 0,
    'UNION apila verticalmente los resultados de dos SELECT compatibles y quita duplicados; UNION ALL los mantiene. No es un JOIN (que combina columnas).'),

  mc('bd-t-048', 'SQL', 3,
    '¿Cuál es la diferencia entre UNION y JOIN?',
    [
      'UNION apila filas de dos consultas (verticalmente); JOIN combina columnas de dos tablas relacionadas (horizontalmente)',
      'UNION combina columnas y JOIN apila filas',
      'Son operaciones idénticas',
      'UNION ordena y JOIN filtra',
    ], 0,
    'UNION agrega filas de resultados con las mismas columnas; JOIN enlaza tablas por una condición y amplía las columnas del resultado.'),

  mc('bd-t-049', 'SQL', 3,
    'El operador IN dentro de un WHERE…',
    [
      'Verifica si un valor está dentro de un conjunto/lista o del resultado de una subconsulta',
      'Une dos tablas por una clave',
      'Ordena las filas del resultado',
      'Cuenta la cantidad de filas',
    ], 0,
    'IN comprueba pertenencia: "WHERE ciudad IN (\'Córdoba\',\'Rosario\')" o "IN (SELECT …)". Equivale a varios OR sobre el mismo campo.'),

  mc('bd-t-050', 'SQL', 3,
    'El operador EXISTS…',
    [
      'Devuelve verdadero si la subconsulta retorna al menos una fila',
      'Cuenta las filas de una tabla',
      'Ordena los resultados',
      'Une dos tablas',
    ], 0,
    'EXISTS evalúa la existencia de filas en una subconsulta correlacionada: en cuanto encuentra una, devuelve verdadero (suele ser eficiente).'),

  mc('bd-t-051', 'SQL', 3,
    'El operador LIKE con el patrón \'A%\' coincide con…',
    [
      'Los valores que empiezan con "A" (el % representa cualquier secuencia de caracteres)',
      'Solo el valor exacto "A%"',
      'Los valores que terminan en "A"',
      'Los valores de un solo carácter',
    ], 0,
    'LIKE busca por patrón: % = cualquier cantidad de caracteres, _ = un solo carácter. \'A%\' = empieza con A; \'%A\' = termina en A.'),

  ms('bd-t-052', 'SQL', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre operadores SQL:',
    [
      'UNION combina los resultados de dos consultas, eliminando duplicados',
      'IN comprueba si un valor pertenece a un conjunto o subconsulta',
      'LIKE busca coincidencias por patrón (con % y _)',
      'HAVING filtra los grupos formados por GROUP BY',
      'UNION une dos tablas relacionándolas por una clave',
      'LIKE compara de forma exacta, sin comodines',
    ], [0, 1, 2, 3],
    'UNION combina resultados, IN comprueba pertenencia, LIKE busca por patrón y HAVING filtra grupos. UNION no es un JOIN y LIKE sí usa comodines.'),

  mc('bd-t-053', 'SQL', 3,
    '¿Qué devuelve "SELECT ciudad, COUNT(*) FROM Cliente GROUP BY ciudad HAVING COUNT(*) > 5;"?',
    [
      'Las ciudades que tienen más de 5 clientes (con su cantidad)',
      'Todas las ciudades, sin filtrar',
      'Los primeros 5 clientes',
      'Las ciudades con 5 clientes o menos',
    ], 0,
    'GROUP BY agrupa por ciudad, COUNT(*) cuenta por grupo y HAVING COUNT(*) > 5 conserva solo los grupos (ciudades) con más de 5 clientes.'),

  mc('bd-t-054', 'Programación BD', 3,
    'Un trigger (disparador) es…',
    [
      'Un bloque de código que se ejecuta automáticamente ante un evento (INSERT/UPDATE/DELETE) sobre una tabla',
      'Una consulta SELECT guardada',
      'Un índice especial',
      'Una restricción UNIQUE',
    ], 0,
    'El trigger reacciona a eventos de datos de forma automática (p. ej. auditar cambios o validar reglas), sin que la aplicación lo invoque explícitamente.'),

  mc('bd-t-055', 'Programación BD', 3,
    'Un procedimiento almacenado (stored procedure)…',
    [
      'Es un conjunto de instrucciones SQL guardado en el servidor que se invoca por su nombre y puede recibir parámetros',
      'Es una tabla temporal de trabajo',
      'Es un tipo de índice',
      'Es un trigger que se dispara solo',
    ], 0,
    'El procedimiento almacenado encapsula lógica en el servidor, reutilizable y parametrizable; se ejecuta explícitamente (CALL/EXEC), a diferencia del trigger.'),

  tf('bd-t-056', 'Programación BD', 3,
    'Un trigger se dispara automáticamente ante un evento sobre una tabla, mientras que un procedimiento almacenado se invoca explícitamente.',
    true,
    'Verdadero. El trigger es reactivo (evento → ejecución automática); el procedimiento es invocado a demanda por la aplicación o por otro código SQL.'),

  mc('bd-t-057', 'Álgebra relacional', 3,
    'En álgebra relacional, la operación de SELECCIÓN (σ)…',
    [
      'Filtra las FILAS (tuplas) que cumplen una condición',
      'Elige columnas (atributos)',
      'Combina dos relaciones',
      'Elimina los valores duplicados',
    ], 0,
    'La selección (σ) se queda con las tuplas que satisfacen un predicado (equivale al WHERE). Elegir columnas es la proyección (π).'),

  mc('bd-t-058', 'Álgebra relacional', 3,
    'La operación de PROYECCIÓN (π) en álgebra relacional…',
    [
      'Elige COLUMNAS (atributos) de una relación',
      'Filtra las filas según una condición',
      'Une dos relaciones',
      'Cuenta la cantidad de filas',
    ], 0,
    'La proyección (π) selecciona un subconjunto de atributos (columnas), equivalente a la lista de campos del SELECT. Filtrar filas es la selección (σ).'),

  ms('bd-t-059', 'Álgebra relacional', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre el álgebra relacional:',
    [
      'La selección (σ) filtra las filas que cumplen una condición',
      'La proyección (π) elige columnas (atributos)',
      'El join combina tuplas de dos relaciones según una condición',
      'El producto cartesiano combina cada tupla de una relación con cada tupla de la otra',
      'La selección (σ) elige columnas de la relación',
      'La proyección (π) filtra las filas',
    ], [0, 1, 2, 3],
    'σ filtra filas, π elige columnas, el join combina por condición y el producto cartesiano combina todas con todas. Las dos últimas intercambian selección y proyección.'),

  mc('bd-t-060', 'SQL vs NoSQL', 3,
    '¿Cuál afirmación sobre bases SQL vs NoSQL es correcta?',
    [
      'Las SQL son relacionales con esquema fijo y ACID fuerte; las NoSQL son flexibles (documentos, clave-valor, grafos), escalan horizontalmente y a veces usan consistencia eventual',
      'Las bases NoSQL son siempre relacionales',
      'Las bases SQL no soportan transacciones',
      'NoSQL significa que no almacenan ningún dato',
    ], 0,
    'Las relacionales (SQL) priorizan esquema y ACID; las NoSQL ofrecen flexibilidad de esquema y escalado horizontal, a menudo relajando la consistencia (eventual) para ganar disponibilidad.'),

  mc('bd-t-061', 'Diseño', 3,
    '¿Cuál es la diferencia entre el modelo lógico y el modelo físico de datos?',
    [
      'El lógico define entidades, atributos y relaciones independiente del SGBD; el físico define tablas, tipos, índices y almacenamiento para un motor concreto',
      'El físico es independiente del motor de base de datos',
      'Son exactamente lo mismo',
      'El lógico es el que define índices y particiones',
    ], 0,
    'El modelo lógico es conceptual/independiente del motor; el físico baja al detalle de implementación de un SGBD específico (tipos, índices, particiones, almacenamiento).'),

  tf('bd-t-062', 'Diseño', 2,
    'El dominio de un atributo es el conjunto de valores válidos que puede tomar (su tipo, rango y formato).',
    true,
    'Verdadero. El dominio define qué valores son admisibles para el atributo; restringirlo (tipo, CHECK, NOT NULL) protege la integridad de los datos.'),

  mc('bd-t-063', 'Diseño', 3,
    'Para almacenar montos de dinero con exactitud conviene usar…',
    [
      'Un tipo decimal/numeric de precisión fija (evitando FLOAT, que tiene errores de redondeo)',
      'FLOAT en todos los casos',
      'VARCHAR para guardarlo como texto',
      'BOOLEAN',
    ], 0,
    'Los tipos de punto flotante (FLOAT/DOUBLE) introducen errores de redondeo; para dinero se usa DECIMAL/NUMERIC con precisión y escala fijas.'),

  mc('bd-t-064', 'Claves', 3,
    '¿Cuál es la diferencia entre una clave natural y una clave subrogada (surrogate)?',
    [
      'La natural tiene significado de negocio (ej. DNI); la subrogada es un identificador artificial sin significado (ej. id autoincremental)',
      'La subrogada tiene significado de negocio y la natural no',
      'Son exactamente lo mismo',
      'La clave natural siempre es autoincremental',
    ], 0,
    'La clave natural surge del dominio (DNI, ISBN); la subrogada es un id generado por el sistema, estable y sin semántica, usado como PK para evitar problemas.'),

  ms('bd-t-065', 'Claves', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre claves naturales y subrogadas:',
    [
      'Una clave subrogada es estable: no cambia aunque cambien los datos del negocio',
      'Una clave natural puede cambiar con el tiempo (ej. un email)',
      'Usar subrogada evita claves primarias compuestas y extensas',
      'La clave natural conviene mantenerla como clave única/alternativa',
      'Una clave natural nunca cambia a lo largo del tiempo',
      'Una clave subrogada tiene significado de negocio',
    ], [0, 1, 2, 3],
    'La subrogada es estable y simple (sin semántica); la natural puede cambiar y conviene conservarla como UNIQUE. Las dos falsas contradicen justamente esas propiedades.'),

  mc('bd-t-066', 'Normalización', 3,
    'Una dependencia funcional A → B significa que…',
    [
      'El valor de A determina unívocamente el valor de B',
      'A y B no tienen ninguna relación',
      'B determina el valor de A',
      'A y B son ambas claves foráneas',
    ], 0,
    'A → B: para cada valor de A hay un único valor de B. Las dependencias funcionales son la base teórica de la normalización (2FN, 3FN, BCNF).'),

  mc('bd-t-067', 'Normalización', 3,
    'La Forma Normal de Boyce-Codd (BCNF)…',
    [
      'Es una versión más estricta de la 3FN: exige que todo determinante sea una clave candidata',
      'Es más débil que la 1FN',
      'Elimina la atomicidad de los atributos',
      'No tiene relación con las dependencias funcionales',
    ], 0,
    'BCNF refuerza la 3FN: en toda dependencia funcional X → Y, X debe ser clave candidata. Resuelve ciertas anomalías que la 3FN aún permite.'),

  tf('bd-t-068', 'Normalización', 3,
    'Normalizar reduce la redundancia y las anomalías de inserción/actualización/borrado, pero puede requerir más JOINs en las consultas.',
    true,
    'Verdadero. Al separar los datos en más tablas se evita la redundancia, pero recomponer la información exige unir tablas (JOINs), con su costo.'),

  mc('bd-t-069', 'Normalización', 3,
    'Una anomalía de actualización ocurre cuando…',
    [
      'Un dato redundante (repetido en muchas filas) se actualiza en unas filas y no en otras, generando inconsistencia',
      'Se pierde un índice de la tabla',
      'Falta una clave foránea',
      'El resultado queda mal ordenado',
    ], 0,
    'Si un mismo dato está duplicado y se cambia solo en algunas filas, la base queda inconsistente. La normalización elimina esa redundancia y previene la anomalía.'),

  ms('bd-t-070', 'Normalización', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre las formas normales:',
    [
      '1FN: atributos atómicos',
      '2FN: sin dependencias parciales de la clave',
      '3FN: sin dependencias transitivas entre atributos no clave',
      'BCNF: todo determinante es una clave candidata',
      'La 2FN elimina las dependencias transitivas',
      'La BCNF es más débil que la 3FN',
    ], [0, 1, 2, 3],
    '1FN=atómico, 2FN=parciales, 3FN=transitivas, BCNF=determinante es clave candidata. Eliminar transitivas es 3FN (no 2FN), y BCNF es MÁS estricta que la 3FN.'),

  mc('bd-t-071', 'Rendimiento', 3,
    'La desnormalización mejora el rendimiento de lectura, pero…',
    [
      'Introduce redundancia y riesgo de inconsistencia, y complica las operaciones de escritura',
      'Siempre mejora todos los aspectos sin contrapartidas',
      'Elimina todos los índices de la base',
      'Reduce el espacio de almacenamiento ocupado',
    ], 0,
    'Al duplicar datos para evitar JOINs, se acelera la lectura pero hay que mantener la redundancia consistente en cada escritura, con más espacio usado.'),

  mc('bd-t-072', 'Índices', 3,
    'Un índice agrupado (clustered)…',
    [
      'Define el orden físico de las filas en la tabla; por eso solo puede haber uno por tabla',
      'No afecta el orden físico de las filas',
      'Puede haber muchos por tabla',
      'Es lo mismo que una vista',
    ], 0,
    'El índice clustered ordena físicamente las filas según su clave; como los datos solo pueden tener un orden físico, hay un único clustered por tabla.'),

  mc('bd-t-073', 'Índices', 3,
    'Un índice no agrupado (non-clustered)…',
    [
      'Es una estructura separada que apunta a las filas; puede haber varios por tabla',
      'Ordena físicamente las filas de la tabla',
      'Solo puede existir uno por tabla',
      'Reemplaza a la clave primaria',
    ], 0,
    'El non-clustered guarda la clave y un puntero a la fila; al ser independiente del orden físico, una tabla puede tener varios.'),

  mc('bd-t-074', 'Índices', 3,
    'En un índice compuesto sobre (apellido, nombre), una búsqueda que filtra solo por "nombre"…',
    [
      'No aprovecha eficientemente el índice: el orden de las columnas importa y se usa de izquierda a derecha (prefijo más a la izquierda)',
      'Lo aprovecha igual de bien que filtrar por apellido',
      'Vuelve el índice inútil también para apellido',
      'Ordena el resultado por nombre primero',
    ], 0,
    'Los índices compuestos se usan por su prefijo izquierdo: filtrar por apellido (o apellido+nombre) sí lo aprovecha; filtrar solo por nombre, no.'),

  tf('bd-t-075', 'Índices', 3,
    'Un índice es más útil en columnas de alta selectividad (muchos valores distintos) que en columnas con pocos valores repetidos (como un booleano).',
    true,
    'Verdadero. Si casi todos los valores son distintos, el índice descarta muchas filas rápido; en una columna con 2 valores apenas filtra, aportando poco.'),

  mc('bd-t-076', 'Rendimiento', 3,
    'El plan de ejecución de una consulta…',
    [
      'Muestra cómo el motor ejecutará la consulta (uso de índices, tipo de join, escaneos) y ayuda a optimizarla',
      'Es el código fuente de la consulta',
      'Es una copia de seguridad de la base',
      'Es un trigger asociado a la tabla',
    ], 0,
    'El plan de ejecución revela la estrategia del optimizador: si usa índices o hace full scan, el orden de los joins, etc. Es clave para diagnosticar consultas lentas.'),

  mc('bd-t-077', 'Rendimiento', 3,
    'Un "escaneo completo de tabla" (full table scan)…',
    [
      'Recorre todas las filas; suele indicar que no se usó un índice y puede ser lento en tablas grandes',
      'Siempre es la opción más rápida',
      'Significa que se usó un índice eficientemente',
      'Es un tipo de JOIN',
    ], 0,
    'El full scan lee toda la tabla. En tablas grandes suele ser síntoma de falta de índice adecuado (aunque para tablas chicas puede ser lo más eficiente).'),

  mc('bd-t-078', 'Recuperación', 3,
    'El registro de transacciones (transaction log / WAL)…',
    [
      'Registra los cambios antes de aplicarlos, permitiendo recuperar la base ante una falla (redo/undo)',
      'Almacena los índices de la base',
      'Es un backup completo de la base',
      'Cifra los datos de las tablas',
    ], 0,
    'El log (write-ahead logging) anota los cambios antes de escribirlos en los datos; tras una caída permite rehacer (redo) los confirmados y deshacer (undo) los no confirmados.'),

  mc('bd-t-079', 'Recuperación', 3,
    'Un checkpoint en una base de datos…',
    [
      'Vuelca a disco los cambios que están en memoria hasta ese punto, acortando el tiempo de recuperación tras una falla',
      'Borra el registro de transacciones',
      'Crea un índice automáticamente',
      'Bloquea por completo la base de datos',
    ], 0,
    'El checkpoint sincroniza memoria y disco hasta un punto; ante una caída, la recuperación solo debe procesar el log posterior al checkpoint, no todo.'),

  ms('bd-t-080', 'Recuperación', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre recuperación y backups:',
    [
      'El log de transacciones permite rehacer/deshacer cambios tras una falla',
      'Un backup completo copia toda la base de datos',
      'Un backup incremental copia solo lo cambiado desde el último respaldo',
      'El checkpoint reduce el tiempo de recuperación tras una caída',
      'Un backup incremental copia toda la base cada vez',
      'El log de transacciones no sirve para la recuperación',
    ], [0, 1, 2, 3],
    'Log (redo/undo), backup completo (todo), incremental (solo cambios) y checkpoint (acelera recuperación) son correctas. Las dos falsas contradicen el incremental y el rol del log.'),

  tf('bd-t-081', 'Recuperación', 2,
    'Las copias de seguridad (backups) periódicas son esenciales para poder recuperar los datos ante fallos, errores humanos o ataques.',
    true,
    'Verdadero. Sin backups, una falla de disco, un borrado accidental o un ransomware pueden provocar la pérdida definitiva de los datos.'),

  mc('bd-t-082', 'OLTP/OLAP', 3,
    '¿Cuál es la diferencia entre OLTP y OLAP?',
    [
      'OLTP procesa transacciones operativas (muchas escrituras cortas, normalizado); OLAP analiza grandes volúmenes para reportes (consultas complejas de lectura, a menudo desnormalizado)',
      'OLTP es analítico y OLAP es transaccional',
      'Son exactamente lo mismo',
      'OLAP solo realiza operaciones INSERT',
    ], 0,
    'OLTP = operación diaria (ventas, altas), optimizado para escrituras y normalizado. OLAP = análisis/BI, optimizado para consultas complejas de lectura sobre grandes volúmenes.'),

  mc('bd-t-083', 'Data Warehouse', 3,
    'Un data warehouse es…',
    [
      'Un repositorio analítico que integra datos históricos de varias fuentes para el soporte a la toma de decisiones',
      'La base de datos operativa de uso diario',
      'Un tipo de índice',
      'Un disparador (trigger)',
    ], 0,
    'El data warehouse consolida e historiza datos de múltiples sistemas para análisis y reportes (BI), separado de las bases operativas (OLTP).'),

  tf('bd-t-084', 'Data Warehouse', 3,
    'Un proceso ETL extrae, transforma y carga datos desde las fuentes hacia el data warehouse.',
    true,
    'Verdadero. ETL (Extract, Transform, Load) obtiene los datos de las fuentes, los limpia/transforma y los carga en el DW con un formato apto para el análisis.'),

  ms('bd-t-085', 'Data Warehouse', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre OLTP, OLAP y data warehouse:',
    [
      'OLTP maneja transacciones operativas (muchas escrituras cortas)',
      'OLAP soporta análisis con consultas complejas de lectura',
      'El data warehouse integra datos históricos de varias fuentes',
      'El proceso ETL extrae, transforma y carga datos hacia el DW',
      'OLAP está optimizado para inserciones masivas en tiempo real',
      'El data warehouse reemplaza a la base de datos operativa',
    ], [0, 1, 2, 3],
    'OLTP=operacional, OLAP=analítico, DW=histórico integrado, ETL=lo alimenta. OLAP no se optimiza para inserciones en tiempo real y el DW complementa (no reemplaza) al OLTP.'),

  mc('bd-t-086', 'Data Warehouse', 3,
    'En un data warehouse, el esquema en estrella se caracteriza por…',
    [
      'Tener una tabla de hechos central rodeada de tablas de dimensiones',
      'No tener ninguna tabla de hechos',
      'Estar completamente normalizado en 3FN',
      'Ser en realidad un índice',
    ], 0,
    'El esquema en estrella ubica los hechos (medidas) en el centro y las dimensiones (contexto: tiempo, producto, cliente) alrededor, optimizando las consultas analíticas.'),

  tf('bd-t-087', 'Data Warehouse', 3,
    'Los data warehouses suelen estar desnormalizados para acelerar las consultas analíticas, a diferencia de las bases OLTP, que se mantienen normalizadas.',
    true,
    'Verdadero. En análisis priman las lecturas: la desnormalización reduce JOINs y acelera reportes; en OLTP, la normalización evita anomalías en las escrituras.'),

  mc('bd-t-088', 'Integridad', 3,
    'La integridad de dominio se asegura mediante…',
    [
      'Restricciones sobre los valores válidos de una columna (tipo de dato, CHECK, NOT NULL)',
      'Únicamente la clave primaria',
      'Solamente los índices',
      'Solo los triggers',
    ], 0,
    'La integridad de dominio limita los valores de cada columna a su dominio válido usando tipos, CHECK, NOT NULL, DEFAULT, etc.'),

  mc('bd-t-089', 'Modelado', 3,
    'En un sistema de biblioteca, un libro puede tener varios autores y un autor escribió varios libros (N:M). ¿Cómo conviene modelarlo?',
    [
      'Con una tabla intermedia Libro_Autor que contenga las FK a Libro y a Autor',
      'Poniendo el autor como una columna repetida en la tabla Libro',
      'Fusionando Libro y Autor en una sola tabla',
      'Usando un único campo con todos los autores separados por comas',
    ], 0,
    'Una relación N:M se resuelve con una tabla asociativa (Libro_Autor) que combina las PK de ambas entidades como FK, evitando redundancia y grupos repetitivos.'),

  mc('bd-t-090', 'Integridad', 3,
    'Para un campo "estado del préstamo" que solo puede valer \'pendiente\', \'activo\' o \'devuelto\', la mejor opción de integridad es…',
    [
      'Una restricción CHECK (o un tipo enumerado / tabla de referencia) que limite los valores válidos',
      'Un campo numérico de texto libre',
      'Un VARCHAR sin ninguna restricción',
      'Un índice agrupado (clustered)',
    ], 0,
    'Restringir el dominio del campo con un CHECK, un ENUM o una tabla de referencia (con FK) impide que se carguen estados inválidos, protegiendo la integridad.'),

  mc('bd-t-091', 'Seguridad', 3,
    'Una inyección SQL (SQL injection) ocurre cuando…',
    [
      'Una entrada del usuario sin sanitizar se concatena en la consulta, permitiendo alterar su lógica',
      'La base de datos se queda sin espacio en disco',
      'Falta un índice en una tabla consultada',
      'Se produce un interbloqueo entre transacciones',
    ], 0,
    'Si la entrada del usuario se concatena directamente en el SQL, un atacante puede inyectar código (ej. " OR 1=1 --") y leer, modificar o borrar datos no autorizados.'),

  mc('bd-t-092', 'Seguridad', 3,
    '¿Cuál es la mejor defensa contra la inyección SQL?',
    [
      'Usar consultas parametrizadas (prepared statements) y validar/escapar la entrada',
      'Concatenar la entrada del usuario directamente en la consulta',
      'Quitar la clave primaria de las tablas',
      'Agregar más índices a las tablas',
    ], 0,
    'Las consultas parametrizadas separan el código SQL de los datos: la entrada se trata como valor, nunca como código ejecutable, neutralizando la inyección.'),

  tf('bd-t-093', 'Seguridad', 2,
    'Aplicar el principio de menor privilegio (dar a cada usuario solo los permisos que necesita) reduce el impacto de una cuenta comprometida.',
    true,
    'Verdadero. Si una cuenta con permisos mínimos es comprometida, el daño posible es acotado; una cuenta con todos los privilegios sería catastrófica.'),

  ms('bd-t-094', 'Seguridad', 3,
    'Seleccioná TODAS las buenas prácticas de seguridad en bases de datos:',
    [
      'Usar consultas parametrizadas para evitar la inyección SQL',
      'Asignar permisos con el principio de menor privilegio (roles)',
      'Cifrar los datos sensibles (en reposo y en tránsito)',
      'Auditar/registrar los accesos a los datos sensibles',
      'Guardar las contraseñas en texto plano',
      'Ejecutar la aplicación web con un usuario administrador de la base',
    ], [0, 1, 2, 3],
    'Parametrizar, menor privilegio, cifrado y auditoría son buenas prácticas. Guardar contraseñas en texto plano o correr la app como admin de la base son fallas graves.'),

  mc('bd-t-095', 'Seguridad', 2,
    'Un rol en una base de datos sirve para…',
    [
      'Agrupar permisos y asignarlos a varios usuarios de forma centralizada',
      'Crear índices automáticamente',
      'Ordenar los resultados de las consultas',
      'Cifrar los datos de forma transparente',
    ], 0,
    'El rol reúne un conjunto de permisos; al asignar el rol a los usuarios se administra el acceso de forma centralizada y consistente.'),

  mc('bd-t-096', 'Seguridad', 3,
    'Las contraseñas de los usuarios deben almacenarse…',
    [
      'Como un hash con salt (no en texto plano ni con cifrado reversible)',
      'En texto plano, para poder recuperarlas si el usuario las olvida',
      'Como un número autoincremental',
      'Dentro del registro de transacciones',
    ], 0,
    'Se guarda un hash (con salt) de la contraseña: irreversible, de modo que ni siquiera el administrador conoce la clave original; el salt evita ataques con tablas precalculadas.'),

  mc('bd-t-097', 'Rendimiento', 3,
    'Una vista materializada, a diferencia de una vista común…',
    [
      'Almacena físicamente el resultado de la consulta y debe refrescarse; acelera lecturas costosas',
      'Nunca almacena datos propios',
      'No se puede consultar como una tabla',
      'Es exactamente lo mismo que un índice',
    ], 0,
    'La vista materializada guarda el resultado calculado (evita recomputarlo en cada consulta), a costa de tener que refrescarla cuando cambian los datos base.'),

  mc('bd-t-098', 'Escalabilidad', 3,
    'El particionamiento de una tabla consiste en…',
    [
      'Dividir una tabla grande en partes (por rango, lista o hash) para mejorar el rendimiento y el manejo',
      'Borrar las filas más antiguas de la tabla',
      'Crear una vista sobre la tabla',
      'Cifrar el contenido de la tabla',
    ], 0,
    'El particionamiento segmenta una tabla en piezas según un criterio (fecha, región, hash); las consultas pueden tocar solo la partición relevante (partition pruning).'),

  mc('bd-t-099', 'Escalabilidad', 3,
    'El sharding (fragmentación horizontal distribuida)…',
    [
      'Reparte las filas de una tabla entre varios servidores para escalar horizontalmente',
      'Duplica toda la base de datos en cada servidor',
      'Es un tipo especial de índice',
      'Cifra la base de datos completa',
    ], 0,
    'El sharding distribuye distintos subconjuntos de filas en nodos diferentes, repartiendo carga y almacenamiento. Copiar los mismos datos en varios nodos es replicación.'),

  mc('bd-t-100', 'Escalabilidad', 3,
    'La replicación en bases de datos…',
    [
      'Mantiene copias de los mismos datos en varios nodos, para disponibilidad y para distribuir las lecturas',
      'Reparte filas distintas entre nodos diferentes',
      'Borra datos para ahorrar espacio',
      'Es un tipo de índice compuesto',
    ], 0,
    'La replicación copia los datos en varios nodos (maestro-réplicas): mejora la disponibilidad y permite repartir las lecturas. Repartir filas distintas es sharding.'),

  tf('bd-t-101', 'Escalabilidad', 3,
    'El sharding reparte datos distintos entre nodos (escala escritura/almacenamiento), mientras que la replicación copia los mismos datos en varios nodos (disponibilidad y lecturas).',
    true,
    'Verdadero. Son estrategias complementarias: sharding = particionar entre nodos; replicación = duplicar en nodos. A menudo se combinan.'),

  mc('bd-t-102', 'NoSQL', 3,
    'El teorema CAP afirma que un sistema distribuido…',
    [
      'No puede garantizar simultáneamente Consistencia, Disponibilidad y Tolerancia a particiones; ante una partición debe elegir entre C y A',
      'Garantiza siempre las tres propiedades a la vez',
      'Solo aplica a las bases relacionales',
      'No tiene relación con los sistemas distribuidos',
    ], 0,
    'CAP: con particiones de red inevitables, el sistema debe sacrificar consistencia o disponibilidad. No se pueden maximizar las tres a la vez.'),

  ms('bd-t-103', 'NoSQL', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre CAP y NoSQL:',
    [
      'CAP: no se pueden garantizar a la vez Consistencia, Disponibilidad y Tolerancia a particiones',
      'Ante una partición de red, hay que elegir entre consistencia y disponibilidad',
      'Muchas bases NoSQL priorizan la disponibilidad con consistencia eventual',
      'Las bases relacionales suelen priorizar la consistencia',
      'CAP garantiza las tres propiedades simultáneamente',
      'La consistencia eventual significa que los datos nunca se consolidan',
    ], [0, 1, 2, 3],
    'CAP obliga a elegir bajo partición; muchas NoSQL eligen disponibilidad (consistencia eventual) y las relacionales, consistencia. Las dos falsas niegan el propio teorema y la idea de "eventual".'),

  mc('bd-t-104', 'NoSQL', 2,
    '¿Cuál de estas NO es un tipo típico de base de datos NoSQL?',
    [
      'Relacional con esquema fijo y SQL',
      'Documental (ej. MongoDB)',
      'Clave-valor (ej. Redis)',
      'Orientada a grafos (ej. Neo4j)',
    ], 0,
    'Las relacionales (SQL) no son NoSQL. Los tipos NoSQL típicos son documental, clave-valor, columnar y de grafos.'),

  ms('bd-t-105', 'NoSQL', 3,
    'Seleccioná TODOS los que son tipos de bases de datos NoSQL:',
    [
      'Documental (ej. MongoDB)',
      'Clave-valor (ej. Redis)',
      'Columnar / familia de columnas (ej. Cassandra)',
      'Orientada a grafos (ej. Neo4j)',
      'Relacional con tablas y SQL',
      'Sistema de archivos jerárquico',
    ], [0, 1, 2, 3],
    'Documental, clave-valor, columnar y de grafos son las cuatro familias NoSQL clásicas. Lo relacional es SQL y un sistema de archivos no es una base de datos NoSQL.'),

  mc('bd-t-106', 'NoSQL', 2,
    'Una base de datos documental almacena los datos como…',
    [
      'Documentos (ej. JSON/BSON) con esquema flexible',
      'Filas y columnas de esquema fijo',
      'Solamente pares clave-valor simples',
      'Nodos y aristas de un grafo',
    ], 0,
    'Las documentales guardan documentos semiestructurados (JSON/BSON) con esquema flexible; cada documento puede tener campos distintos.'),

  mc('bd-t-107', 'SQL', 3,
    'Un autojoin (self-join) se utiliza para…',
    [
      'Relacionar filas de una tabla consigo misma (ej. empleado y su supervisor en la misma tabla Empleado)',
      'Unir dos tablas distintas por una clave',
      'Ordenar las filas de una tabla',
      'Cifrar los datos de una columna',
    ], 0,
    'El self-join une la tabla consigo misma usando alias distintos, útil para jerarquías o relaciones reflexivas (supervisor, categoría padre, etc.).'),

  mc('bd-t-108', 'SQL', 3,
    'En SQL, la función COUNT(columna) (con una columna específica)…',
    [
      'Cuenta solo los valores NO nulos de esa columna (a diferencia de COUNT(*), que cuenta todas las filas)',
      'Cuenta también las filas con valor NULL',
      'Da error si la columna tiene NULL',
      'Cuenta solo los valores distintos',
    ], 0,
    'COUNT(columna) ignora los NULL; COUNT(*) cuenta todas las filas sin importar nulos. Para únicos se usa COUNT(DISTINCT columna).'),

  tf('bd-t-109', 'SQL', 3,
    'Las funciones de agregado como SUM o AVG ignoran los valores NULL en sus cálculos.',
    true,
    'Verdadero. Los NULL se excluyen del cálculo: AVG promedia solo los valores presentes (no los cuenta como cero), y SUM los omite.'),

  mc('bd-t-110', 'SQL', 3,
    '¿Cuál es la diferencia entre COUNT(*) y COUNT(email) en una tabla Cliente?',
    [
      'COUNT(*) cuenta todas las filas; COUNT(email) cuenta solo las filas cuyo email no es nulo',
      'Siempre devuelven el mismo número',
      'COUNT(*) ignora los valores nulos',
      'COUNT(email) cuenta únicamente los nulos',
    ], 0,
    'Si algunos clientes no tienen email (NULL), COUNT(email) será menor que COUNT(*), porque COUNT de una columna omite los nulos.'),

  mc('bd-t-111', 'SQL', 3,
    'En "SELECT vendedor, SUM(total) FROM Venta WHERE total>0 GROUP BY vendedor HAVING SUM(total)>10000;", ¿qué hace cada parte?',
    [
      'WHERE filtra filas antes de agrupar; GROUP BY agrupa por vendedor; HAVING filtra los grupos con suma mayor a 10000',
      'HAVING filtra las filas antes de agrupar',
      'WHERE filtra los grupos ya formados',
      'No se pueden combinar WHERE y HAVING en una consulta',
    ], 0,
    'WHERE actúa fila por fila (total>0), luego GROUP BY agrupa por vendedor y, finalmente, HAVING filtra los grupos según el agregado (SUM(total)>10000).'),

  mc('bd-t-112', 'Programación BD', 3,
    'Un cursor en SQL…',
    [
      'Permite recorrer fila por fila el resultado de una consulta (procesamiento iterativo)',
      'Es un tipo de índice',
      'Es una vista materializada',
      'Es una restricción de integridad',
    ], 0,
    'El cursor procesa los resultados de a una fila; es útil para lógica fila a fila, aunque suele ser menos eficiente que el procesamiento basado en conjuntos.'),

  tf('bd-t-113', 'Rendimiento', 3,
    'El procesamiento basado en conjuntos (set-based) suele ser más eficiente que recorrer las filas una por una con un cursor.',
    true,
    'Verdadero. El motor optimiza mejor las operaciones sobre conjuntos completos; los cursores (fila por fila) agregan overhead y suelen ser más lentos.'),

  mc('bd-t-114', 'Transacciones', 3,
    'Un SAVEPOINT dentro de una transacción permite…',
    [
      'Marcar un punto al que hacer un rollback parcial, sin deshacer toda la transacción',
      'Confirmar definitivamente la transacción',
      'Crear un índice sobre una tabla',
      'Cifrar los datos de la transacción',
    ], 0,
    'El SAVEPOINT crea un punto intermedio; un ROLLBACK TO savepoint deshace solo lo posterior a ese punto, conservando el resto de la transacción.'),

  ms('bd-t-115', 'Transacciones', 3,
    'Seleccioná TODAS las afirmaciones correctas sobre el control de transacciones:',
    [
      'COMMIT confirma (hace permanentes) los cambios de la transacción',
      'ROLLBACK deshace los cambios de la transacción',
      'Un SAVEPOINT permite hacer un rollback parcial dentro de la transacción',
      'Una transacción agrupa operaciones como una unidad atómica (todo o nada)',
      'COMMIT deshace los cambios realizados',
      'ROLLBACK confirma de forma permanente los cambios',
    ], [0, 1, 2, 3],
    'COMMIT confirma, ROLLBACK deshace, SAVEPOINT permite rollback parcial y la transacción es atómica. Las dos falsas intercambian el significado de COMMIT y ROLLBACK.'),

  mc('bd-t-116', 'Índices', 3,
    'Un índice "cubriente" (covering index)…',
    [
      'Contiene todas las columnas que la consulta necesita, evitando tener que ir a la tabla',
      'No contiene ninguna columna de datos',
      'Cifra la consulta antes de ejecutarla',
      'Es en realidad una vista',
    ], 0,
    'Si el índice ya incluye todas las columnas requeridas (filtro y proyección), el motor responde solo con el índice, sin leer la tabla: consulta más rápida.'),

  mc('bd-t-117', 'Rendimiento', 3,
    'Una consulta que filtra por la columna "email" en una tabla grande es lenta (hace full table scan). ¿Qué conviene hacer?',
    [
      'Crear un índice sobre la columna email',
      'Borrar la clave primaria de la tabla',
      'Quitar la cláusula WHERE de la consulta',
      'Desnormalizar toda la base de datos',
    ], 0,
    'Un índice sobre email permite localizar las filas directamente en vez de recorrer toda la tabla, eliminando el full scan y acelerando la consulta.'),

  tf('bd-t-118', 'Índices', 3,
    'Crear demasiados índices puede perjudicar el rendimiento de las escrituras, porque cada INSERT/UPDATE/DELETE debe mantenerlos actualizados.',
    true,
    'Verdadero. Cada índice extra agrega trabajo en cada escritura y ocupa espacio; conviene indexar lo necesario, no de más.'),

  ms('bd-t-119', 'Repaso', 3,
    'Repaso integrador: marcá TODAS las afirmaciones correctas:',
    [
      'La clave primaria identifica unívocamente cada fila',
      'La clave foránea garantiza la integridad referencial',
      'Las transacciones cumplen las propiedades ACID',
      'Un índice acelera las lecturas a costa de escrituras más lentas',
      'Una vista común almacena siempre los datos físicamente',
      'La normalización aumenta la redundancia de los datos',
    ], [0, 1, 2, 3],
    'PK única, FK integridad referencial, transacciones ACID e índices (lectura↑/escritura↓) son correctas. La vista común NO almacena datos (es virtual) y la normalización REDUCE la redundancia.'),

  mc('bd-t-120', 'Modelado', 3,
    'En un sistema de cursos, un alumno se inscribe en muchos cursos y querés guardar la NOTA y la FECHA de inscripción. ¿Cómo modelás la relación?',
    [
      'Con una tabla intermedia Inscripción (alumno_id, curso_id, nota, fecha)',
      'Agregando columnas nota1, nota2, nota3… en la tabla Alumno',
      'Guardando la nota en la tabla Curso',
      'Con un único campo de texto en Alumno que liste los cursos',
    ], 0,
    'Es una relación N:M con atributos propios (nota, fecha): se modela con una tabla asociativa (Inscripción) que lleva las FK a Alumno y Curso más esos atributos.'),
]);
